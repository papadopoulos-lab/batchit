% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch.R
\name{batch_run}
\alias{batch_run}
\title{Run a target on each of a fixed list of items, one subprocess per item}
\usage{
batch_run(
  target,
  items,
  n_workers,
  dev_path = NULL,
  collect = TRUE,
  p = NULL,
  label = NULL,
  timeout = .BATCH_DEFAULT_TIMEOUT
)
}
\arguments{
\item{target}{A \code{batch_target} descriptor from \code{\link[=batch_target]{batch_target()}}.}

\item{items}{List of items; each a fully-named list of the target's formals.
Named items keep their name as the item id; unnamed items get their index.}

\item{n_workers}{Concurrent subprocesses (validated: finite, whole, >= 1).}

\item{dev_path}{Consumer-package source tree for \code{devtools::load_all()} in the
worker, or \code{NULL} for the installed consumer package. A given-but-wrong path
errors rather than silently falling back to installed code.}

\item{collect}{If \code{TRUE}, return each item's value in item order; if \code{FALSE},
the worker still reports status but its value never crosses back (for
targets that write their output themselves).}

\item{p}{A progress callback such as a \code{progressr} progressor, or \code{NULL}. It
is called once per completed item with \verb{message = <id and time>}.}

\item{label}{Optional short stage tag prefixed to the progress message.}

\item{timeout}{Per-item wall-clock limit in seconds; a worker that exceeds it
is killed and reported as a failure. Defaults to a generous hang-catcher
(the internal \code{.BATCH_DEFAULT_TIMEOUT}, 6 hours); pass \code{Inf} to disable.}
}
\value{
If \code{collect}, a list of values in item order; else \code{invisible(NULL)}.
}
\description{
Shape A of the contract: the items already exist (each a small named list of
the target's formals; the worker opens its own data), so a fresh R process per
item is not a cost to amortise but the memory strategy itself -- a large
analysis item can peak at tens of GB and R does not return that memory to the
OS, so process exit is how it is reclaimed. This is why batchit does NOT reuse
workers: worker reuse would defeat exactly this.
}
\details{
Both-end validation, a hash-verified target descriptor, per-item logs written
to files (never pipes -- a chatty worker filling the OS pipe buffer is what
deadlocks a pipe transport), a bounded log tail on failure, and a loud stop on
the first failure. Warnings a target captures are surfaced in the parent,
tagged by item id.

batchit is thread-agnostic: it sets no BLAS / data.table thread counts and
passes none to the worker. If a target is itself multi-threaded, dividing
cores across \code{n_workers} (to avoid oversubscription) is the CONSUMER's
responsibility, not the runner's.

The worker script is always the runner's (batchit's); \code{dev_path}, when given,
is the CONSUMER's source tree. When runner and consumer differ, the worker
loads both (the consumer via \code{dev_path}/\code{requireNamespace}, the runner via
\code{requireNamespace}).
}
\examples{
\dontrun{
t <- batch_target("mypkg", "process_one_slice")
out <- batch_run(t, items = list(list(x = 1), list(x = 2)), n_workers = 2)
}
}
