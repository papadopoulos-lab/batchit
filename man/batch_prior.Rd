% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch_task.R
\name{batch_prior}
\alias{batch_prior}
\title{Return the PRIOR run's \code{batch_record()} details for THIS item}
\usage{
batch_prior()
}
\value{
The prior \code{details} value, or \code{NULL}.
}
\description{
Callable ONLY from inside the target of a \code{run_and_write_files_atomically()} item, exactly
like \code{\link[=batch_record]{batch_record()}}. Returns the \code{details} value THIS SAME item's PRIOR
successful commit passed to \code{batch_record()}, captured at STEP 0 -- before
\code{do.call()} even started (design PHASE6_DESIGN.md section 9.2) -- from the
final marker at this item's derived marker path, but ONLY if that marker
decodes AND its protocol, attempt token, and committed output map all
verify against THIS item's own declared outputs; a malformed, foreign,
absent, or symlinked marker, or one belonging to a differently-shaped
item, all make this return \code{NULL}, exactly like there being no prior at
all. A VALID prior marker whose \code{details} is itself \code{NULL}/absent ALSO
makes this return \code{NULL} -- i.e. a target that never called
\code{batch_record()} on its prior run disables skip for the next one, since
there is nothing for the target to decide currency from.
}
\details{
The target is expected to inspect this value and decide for itself
whether the prior outputs are still current; if so, it should RETURN
\code{\link[=batch_skip]{batch_skip()}} instead of recomputing. batchit ascribes NO meaning to
\code{details} and never makes this decision itself (design section 0).
}
\examples{
\dontrun{
my_target <- function(x) {
  prior <- batch_prior()
  if (!is.null(prior) && identical(prior$computed_from, x)) {
    return(batch_skip())
  }
  batch_record(list(computed_from = x))
  list(primary = x)
}
}
}
