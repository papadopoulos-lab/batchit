% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch_adhoc.R
\name{batch_fn}
\alias{batch_fn}
\title{Run a bare closure on each of a fixed list of items, one subprocess per item}
\usage{
batch_fn(
  fn,
  items,
  n_workers,
  dev_path = NULL,
  collect = FALSE,
  p = NULL,
  label = NULL,
  timeout = .BATCH_DEFAULT_TIMEOUT
)
}
\arguments{
\item{fn}{A bare closure: self-contained (base R, \verb{pkg::}-qualified calls,
and its own formals only -- see \code{.batch_lint_adhoc_fn()}), not a primitive,
and not taking \code{...}.}

\item{items}{List of items; each a fully-named list of \code{fn}'s formals.
Named items keep their name as the item id; unnamed items get their
index.}

\item{n_workers}{Concurrent subprocesses (validated: finite, whole, >= 1).}

\item{dev_path}{For \code{adhoc} dispatch this names BATCHIT'S OWN source tree
(loaded via \code{devtools::load_all()} in the worker), NOT a consumer
package -- an adhoc closure has no separate consumer identity to load,
so there is nothing else \code{dev_path} could sensibly name here. A
given-but-wrong path errors rather than silently falling back to the
installed package. \code{NULL} (the default) uses the installed \code{batchit},
which is what any downstream caller wants; a non-\code{NULL} value is for
developing/testing batchit's own \code{adhoc} code without reinstalling it
first. A closure's own \code{pkg::fun()} calls are unaffected either way --
they resolve via ordinary lazy namespace loading in the worker, exactly
like any other R code.}

\item{collect}{If \code{TRUE}, return each item's value in item order; if
\code{FALSE} (the default here), the worker still reports status but its
value never crosses back.}

\item{p}{A progress callback such as a \code{progressr} progressor, or \code{NULL}.}

\item{label}{Optional short stage tag prefixed to the progress message.}

\item{timeout}{Per-item wall-clock limit in seconds; see \code{\link[=batch_run]{batch_run()}}.}
}
\value{
If \code{collect}, a list of values in item order; else
\code{invisible(NULL)}.
}
\description{
The \code{adhoc} sibling of \code{\link[=batch_run]{batch_run()}} (design PHASE6_DESIGN.md sections 1-2,
4-5, 9.4): the SAME transport (a fresh \code{processx} subprocess per item, the
same \code{inst/batch_worker.R}, the same both-ends item validation and
result-envelope inspection), but \code{fn} is a bare closure VALUE -- serialized
straight into the envelope -- instead of a \code{\link[=package_function]{package_function()}} descriptor
resolved by package+symbol.
}
\details{
The closure is gated by a best-effort static self-containedness LINT (see
\code{.batch_lint_adhoc_fn()}): it may reference only base R (including base
operators/constants), its own declared formals, and explicit
\code{pkg::fun()}/\code{pkg:::fun()} calls -- any other free variable is rejected,
NAMING it, before any subprocess is launched. \code{...} in \code{fn}'s formals is
prohibited, exactly like a package \code{\link[=package_function]{package_function()}}. Once accepted, the
closure is unconditionally rebased onto \code{baseenv()} before it is ever
serialized (see \code{.batch_rebase_adhoc_closure()}) -- there is no
env-preservation mode, so anything the closure needs beyond base R + its
own formals + \verb{pkg::}-qualified calls must be passed as an item argument.

Because an adhoc closure carries no package/symbol/hash identity, a result
is instead bound to the dispatch that produced it via the item id (already
checked) PLUS a fresh, high-entropy per-item nonce issued here and echoed
back by the child -- see \code{.batch_inspect_result()}'s \code{expected_nonce}.

Production/auditable stages should prefer \code{\link[=batch_run]{batch_run()}} with a
\code{\link[=package_function]{package_function()}} descriptor (hash-verified, resolvable by package+symbol,
so a stale/wrong code version is caught); \code{adhoc} dispatch is for
throwaway/exploratory work where that overhead is not the point. The lint
is a best-effort static check, not a proof -- see \code{.batch_lint_adhoc_fn()}
for its documented blind spots.
}
\examples{
\dontrun{
batch_fn(
  function(x) x * 2,
  items = list(list(x = 1), list(x = 2)),
  n_workers = 2, collect = TRUE
)
}
}
