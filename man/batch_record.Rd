% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch_task.R
\name{batch_record}
\alias{batch_record}
\title{Attach an opaque \code{details} value to THIS item's new marker}
\usage{
batch_record(details)
}
\arguments{
\item{details}{Any value \code{\link[qs2:qs_save]{qs2::qs_save()}} can serialize.}
}
\value{
\code{invisible(NULL)}.
}
\description{
Callable ONLY from inside the target of a \code{run_and_write_files_atomically()} item (i.e. only
in a batchit worker subprocess, while that one target call is running,
between the \code{.batch_record_scope_enter()}/\verb{_exit()} pair tightly wrapping
\code{do.call()} in \code{.batch_execute()}) -- the opt-in half of the consumer-skip
mechanism (design PHASE6_DESIGN.md section 7): the target may call this
any number of times during its own run; the LAST call wins (design
section 9.3). \code{details} is entirely opaque to batchit -- never inspected,
compared, or interpreted (design section 0) -- it travels straight into
the item's committed marker (\code{.batch_commit_task()}'s \code{details} field) and
is handed back verbatim to a LATER run of the SAME item via
\code{\link[=batch_prior]{batch_prior()}}. It must be something \code{\link[qs2:qs_save]{qs2::qs_save()}} can serialize; an
unserializable (or otherwise invalid) value fails the target's own commit
at the marker-temp-write step (design section 9.3) -- BEFORE the old
marker is removed, so a failing \code{batch_record()} payload can never destroy
a previously valid commit.
}
\details{
Calling this outside an active \code{run_and_write_files_atomically()} target run -- including
during a return-value dispatch (\code{\link[processx:run]{run()}}/\code{\link[=run_and_collect]{run_and_collect()}}/\code{\link[=batch_stream]{batch_stream()}}),
which has no marker/skip machinery at all -- is an error.
}
\examples{
\dontrun{
# inside a run_and_write_files_atomically() target:
my_target <- function(x) {
  batch_record(list(computed_from = x))
  list(primary = x)
}
}
}
