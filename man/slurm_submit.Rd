% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/slurm.R
\name{slurm_submit}
\alias{slurm_submit}
\title{Submit a written Slurm job chain}
\usage{
slurm_submit(x)
}
\arguments{
\item{x}{What to run. Three shapes reach the same driver.
\enumerate{
\item The character vector \code{\link[=slurm_write]{slurm_write()}} returned. Its last element is
the \code{submit.sh} path.
\item A directory that holds \code{submit.sh}.
\item The path of a \code{submit.sh}.
}

Any other value is an error. The named file MUST be \code{submit.sh}: a job
file runs under Slurm, so running one here would run the job in this
session.}
}
\value{
Named character vector of job ids, in chain order. The names are
the stage names.
}
\description{
Runs a \code{submit.sh} that \code{\link[=slurm_write]{slurm_write()}} wrote, and returns the job ids the
driver submitted. The names of the returned vector are the stage names.
}
\details{
\code{slurm_write()} writes the chain and submits nothing. \code{slurm_submit()}
submits it. They stay separate calls, so a caller reads \code{submit.sh}
between them:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{paths <- slurm_write(jobs, dir)
writeLines(readLines(paths[[length(paths)]]))
ids <- slurm_submit(paths)
}\if{html}{\out{</div>}}
}
\section{Where the ids come from}{

The driver prints one line for each submission, and \code{slurm_submit()} reads
those lines:

\if{html}{\out{<div class="sourceCode">}}\preformatted{batchit_submitted proj_s1 5512
batchit_submitted proj_s2 5513
}\if{html}{\out{</div>}}

Nothing reads \code{sbatch}'s own output. The driver pipes that output through
\verb{cut -d';' -f1}, because a federated cluster writes \verb{jobid;cluster} there.
}

\section{What an error carries}{

A driver that exits non-zero stops this call. The error carries the
driver's standard error verbatim, because that text holds the diagnosis
and the repair. A drained node reports the \verb{scontrol update} command that
resumes it.

The driver runs under \verb{set -euo pipefail}. So an \code{sbatch} that fails part
way through a chain stops the driver with the earlier jobs ALREADY QUEUED.
The error then names each queued stage and its id, and it gives the
\code{scancel} command that cancels them.
}

\section{Two calls cannot both submit the same chain}{

The generated driver refuses a job name it finds in \code{squeue}, and it fails
closed. \code{slurm_submit()} adds no second check. One policy in one place
cannot disagree with itself.
}

\examples{
\dontrun{
jobs <- list(
  slurm_it(
    script = "Rscript s1_build.R",
    name = "proj_s1",
    cpus = 6,
    mem = "85G",
    time = "12:00:00"
  ),
  slurm_it(
    script = "Rscript s2_report.R",
    name = "proj_s2",
    cpus = 2,
    mem = "8G",
    time = "01:00:00"
  )
)
paths <- slurm_write(jobs, "~/chain")
slurm_submit(paths)
}
}
\seealso{
\code{vignette("batchit")}, section "Slurm: write a chain of
jobs".

Other slurm:
\code{\link[=slurm_it]{slurm_it()}},
\code{\link[=slurm_write]{slurm_write()}}
}
\concept{slurm}
