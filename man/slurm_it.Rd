% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/slurm_it.R
\name{slurm_it}
\alias{slurm_it}
\title{Describe one Slurm job}
\usage{
slurm_it(
  script,
  name,
  cpus,
  mem,
  time,
  requeue = TRUE,
  exclusive = FALSE,
  require_r_package = character(0),
  sbatch = character(0)
)
}
\arguments{
\item{script}{Character(1). The shell command the job runs. This is the one
field that MAY hold more than one line, because it becomes the body of
the generated script rather than a directive. It MUST hold at least one
character that is not whitespace.}

\item{name}{Character(1). The job identity. It names the job to Slurm, and
it names three files: \verb{<name>.sh}, \verb{<name>_\%j.out} and \verb{<name>_\%j.err}.
It MUST match \verb{^[A-Za-z0-9][A-Za-z0-9_.-]*$}. The leading character rule
is the load-bearing half: a name that starts with \code{-} reads as an option.}

\item{cpus}{Character(1) or one number. The core count, which reaches
\code{--cpus-per-task}. It MUST be one non-negative whole number.}

\item{mem}{Character(1) or one number. The memory request in Slurm's own
notation, such as \code{"85G"}. It MUST be digits, then an optional \code{K}, \code{M},
\code{G} or \code{T}.}

\item{time}{Character(1). The wall-clock limit, as \code{HH:MM:SS} or
\code{D-HH:MM:SS}. No other format is accepted.}

\item{requeue}{Logical(1). \code{TRUE} asks Slurm to requeue the job after a
node failure. \code{FALSE} asks Slurm not to.}

\item{exclusive}{Logical(1). \code{TRUE} asks for the whole node.}

\item{require_r_package}{Named character vector. Each name is an R package
the job needs, and each value is the version that package MUST be at.
Defaults to \code{character(0)}.}

\item{sbatch}{Named character vector of extra \code{sbatch} long options,
written without the leading \verb{--}. A name in the reserved list is an
error, because a formal argument or the job chain already sets it. The
reserved names are \code{job-name}, \code{cpus-per-task}, \code{mem}, \code{time}, \code{output},
\code{error}, \code{exclusive}, \code{requeue}, \code{no-requeue} and \code{dependency}. Each value
MUST hold at least one character that is not whitespace. Defaults to
\code{character(0)}.}
}
\value{
An object of class \code{slurm_it}. It is a list with the elements
\code{script}, \code{name}, \code{cpus}, \code{mem}, \code{time}, \code{requeue}, \code{exclusive},
\code{require_r_package} and \code{sbatch}. \code{cpus} and \code{mem} come back as text,
whichever type the caller passed.
}
\description{
Builds a validated description of one Slurm job. It writes nothing, because
\code{slurm_write()} is what turns the object into a job script.
}
\details{
Every field this object carries reaches a \verb{#SBATCH} directive or a
generated script, so this function checks all of them before it returns.
The checks reject a line break, a whitespace character and a shell
metacharacter, each in the fields where that character would change what
runs.
}
\examples{
job <- slurm_it(
  script = "Rscript s1_build.R",
  name = "proj_s1",
  cpus = 6,
  mem = "85G",
  time = "12:00:00"
)
job[["name"]]
job[["cpus"]]
}
\seealso{
\code{vignette("batchit")}, section "Slurm: write a chain of
jobs".

Other slurm:
\code{\link[=inside_slurm_job]{inside_slurm_job()}},
\code{\link[=slurm_status]{slurm_status()}},
\code{\link[=slurm_submit]{slurm_submit()}},
\code{\link[=slurm_write]{slurm_write()}}
}
\concept{slurm}
