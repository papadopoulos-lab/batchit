[{"path":"https://papadopoulos-lab.github.io/batchit/PHASE6_DESIGN.html","id":null,"dir":"","previous_headings":"","what":"Phase 6′ design (v3) — batchit function dispatch + output-commit","title":"Phase 6′ design (v3) — batchit function dispatch + output-commit","text":"Consolidated maintainer reframes + codex design rounds 1–2. Doctrine (settled): batchit dispatches delivers outputs; holds staleness opinion . Correctness + ergonomics . Baseline batchit 26.7.19 (HEAD 235174f).","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/PHASE6_DESIGN.html","id":"id_0-normative-doctrine-invariant-codex-supplied-test-around-it","dir":"","previous_headings":"","what":"0. Normative doctrine invariant (codex-supplied; test around it)","title":"Phase 6′ design (v3) — batchit function dispatch + output-commit","text":"batchit launch decision may depend marker existence contents. Marker reads permitted completing verifying already-dispatched commit, hand consumer prior details (§7). marker carries batchit-computed input hash, code hash, mtime, provenance, “need--run” state — protocol number, random attempt token, committed output names, (§7) OPAQUE consumer-supplied details value batchit ascribes meaning . lockdown test asserts batchit never branches dispatch marker state.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/PHASE6_DESIGN.html","id":"id_1-the-reframe--dispatch-is-a-function-on-two-orthogonal-axes","dir":"","previous_headings":"","what":"1. The reframe — dispatch is a function on two orthogonal axes","title":"Phase 6′ design (v3) — batchit function dispatch + output-commit","text":"Envelope meta carries: - fn_kind — \"package\" (package+symbol+body/formals hash; hash-verified ends; auditable; production stages) \"adhoc\" (serialized closure; static-lint gated; ad-hoc ). - outputs (optional) — absent → return value (today’s batch_run, collect-gated); present → declared-output commit (§3), result small commit record, never raw data. Independent axes; discriminated envelope codex required. .BATCH_PROTOCOL bumps; old executor rejects new-shaped envelope (existing check R/batch.R:258); worker checks protocol conditionally loading consumer package.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/PHASE6_DESIGN.html","id":"id_2-frontends","dir":"","previous_headings":"","what":"2. Frontends","title":"Phase 6′ design (v3) — batchit function dispatch + output-commit","text":"batch_run(target, items, …) — unchanged (package fn, return value). batch_task(fn, items, outputs, style, …) — package|adhoc fn + declared-output commit. collect. fn = batch_target() descriptor (→ package) bare closure (→ adhoc). batch_fn(fn, items, …) — adhoc fn + return value. Distinct public contracts; shared internal engine.","code":""},{"path":[]},{"path":"https://papadopoulos-lab.github.io/batchit/PHASE6_DESIGN.html","id":"id_31-outputs-spec-per-item-never-in-target-args--conservative-path-validation","dir":"","previous_headings":"3. Declared-output commit (batch_task)","what":"3.1 Outputs spec (per item; never in target args) + conservative path validation","title":"Phase 6′ design (v3) — batchit function dispatch + output-commit","text":"outputs = named character vector c(<name> = <final path>). Lives meta, never args. -ends validation, conservatively scoped (codex 5): - non-empty; unique names; unique paths; absolute; normalizePath(mustWork=FALSE)-normalized. - destination absent existing non-directory, non-symlink file (dirs + symlinks rejected; base R portably detect FIFO/socket/device special file, destination rejected may replaced commit rename — documented, guarantee). - parent dir output/marker/stage path must already exist. - within ONE invocation, reject output path aliases another (item concurrent item) — normalize .., resolve existing symlinks, check hardlink identity platform exposes . Explicitly promised: cross-invocation/cross-process exclusivity (global lock), case-fold/Unicode canonicalization arbitrary mounts, absent path later become alias. Contract assumes single exclusive writer per output; TOCTOU validate rename documented boundary.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/PHASE6_DESIGN.html","id":"id_32-two-styles-external_writer-dropped--both-implemented-unit-1-return-unit-2-staged_writer","dir":"","previous_headings":"3. Declared-output commit (batch_task)","what":"3.2 Two styles (external_writer dropped) — BOTH IMPLEMENTED (Unit 1 return, Unit 2 staged_writer)","title":"Phase 6′ design (v3) — batchit function dispatch + output-commit","text":"return codec qs2, always. arbitrary writer closures spec. staged_writer‘s scoped accessor backed package-level environment (.batch_stage_env), entered/exited around .call .batch_execute() (never inside .batch_commit_task(), since paths must exist target runs) — see batch_stage_path() .batch_stage_paths_for() R/batch_task.R. parent-side sweep (.batch_sweep_task_temps()) matches .<attempt>.tmp .<attempt>.stage, covering styles’ leftover temps one SIGKILL/timeout cleanup path.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/PHASE6_DESIGN.html","id":"id_33-commit-sequence--child-side-in-batch_execute-after-docall-codex-4-order","dir":"","previous_headings":"3. Declared-output commit (batch_task)","what":"3.3 Commit sequence — child-side, in .batch_execute after do.call (codex 4 order)","title":"Phase 6′ design (v3) — batchit function dispatch + output-commit","text":"Run target; prepare every output temp (return: qs2-write ; staged: assert every batch_stage_path(name) exists regular non-symlink file — undeclared/absent name → error, zero renames). Prepare + close marker temp, embedding fresh random attempt token (+ protocol, committed names, §7 details). Verify every temp (outputs + marker) regular non-symlink file. Remove old final marker verify removal succeeded (stale marker must survive attempt — §7 read step 0 already captured prior details). Replace every final output (rename temp → final). Rename marker temp → final marker path LAST (atomic commit point — last namespace op). Read back final marker verify attempt token; emit commit record. Cleanup unconditional every failure path (target error staged files; partial return serialization; failure mid output-rename loop; marker temp/rename failure; timeout/kill far parent can reach): remove remaining temps; never roll back guessing old finals recoverable. Preparing everything step 4 keeps invalid window minimal stops serialization/marker failure needlessly destroying prior valid witness. Replacement primitive: file.rename() overwrite semantics differ across POSIX/Windows/CIFS; unlink--rename opens missing-file window. Contract states supported semantics = POSIX + server-side-atomic CIFS rename-replace; platform-atomic-replace helper supplied/tested, production target (uppsala + CIFS) tested path.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/PHASE6_DESIGN.html","id":"id_34-batch_stage_pathname-staged_writer-only","dir":"","previous_headings":"3. Declared-output commit (batch_task)","what":"3.4 batch_stage_path(name) (staged_writer only)","title":"Phase 6′ design (v3) — batchit function dispatch + output-commit","text":"Returns exact per-output sibling temp path batchit rename . Backed scoped option set .call duration (set/restore + unconditional cleanup). Rejects undeclared name; errors outside staged run; partial stages cleaned failure.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/PHASE6_DESIGN.html","id":"id_35-result-record-codex-3-fix","dir":"","previous_headings":"3. Declared-output commit (batch_task)","what":"3.5 Result record (codex 3 fix)","title":"Phase 6′ design (v3) — batchit function dispatch + output-commit","text":"commit record echoes named output map actually committed: list(committed = <named char: name -> final path>, attempt = <token>). parent inspector compares committed (names paths) + attempt dispatched outputs + token issued — bare name list. raw value ever crosses back (batch_task collect; target return unconditionally discarded child-side commit, styles).","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/PHASE6_DESIGN.html","id":"id_4-discriminated-envelope--validation-matrix-codex-3--implemented-unit-3","dir":"","previous_headings":"","what":"4. Discriminated envelope + validation matrix (codex 3) — IMPLEMENTED (Unit 3)","title":"Phase 6′ design (v3) — batchit function dispatch + output-commit","text":"fn_kind REQUIRED (\"package\"|\"adhoc\"); unknown/missing → reject ends. package: package+symbol+hash required; dev_path valid (names CONSUMER’s tree). adhoc: closure (meta$fn) + per-dispatch nonce (meta$nonce) required; package/symbol/hash (forbidden, directions — package-kind envelope carrying fn/nonce rejected ); worker preflight conditional fn_kind (inst/batch_worker.R’s .batch_worker_check() requires .function(meta$fn) adhoc instead package/symbol/hash); formals + args get -end validation package items (every arg named formal; positional; ... prohibited, targets) via .batch_validate_adhoc_item(). dev_path adhoc names BATCHIT’S tree (consumer identity exists validate ) — resolved decision, see §5 note . style required EXACTLY outputs present; forbidden otherwise. styles (return, staged_writer) work either fn_kind — style fn_kind independent axes, designed. Unknown envelope fields rejected, ignored.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/PHASE6_DESIGN.html","id":"id_5-adhoc--the-static-self-containedness-lint-not-proof--implemented-unit-3","dir":"","previous_headings":"","what":"5. adhoc + the static self-containedness LINT (not proof) — IMPLEMENTED (Unit 3)","title":"Phase 6′ design (v3) — batchit function dispatch + output-commit","text":"codetools::findGlobals(fn, merge=FALSE) — inspect $variables $functions. Reject global {base/implicit-base + base operators, pkg::/pkg::: qualified reference}. Implemented .batch_lint_adhoc_fn() (R/batch_adhoc.R), applied ends: frontend (batch_fn() / batch_task() bare closure) dispatch time, .batch_check_envelope() CHILD. Verified empirically: pkg::fun()/pkg:::fun() calls NEVER independently flagged codetools (::/::: call reported, base), special-casing needed qualified-call carve-. Mandatory rebase (accepted) closure’s environment baseenv() serialization — simpler “optional,” closes large/secret enclosing-environment carriage path. (env-preservation mode; ever needed loud documented adhoc limitation.) Implemented .batch_rebase_adhoc_closure(); CHILD re-rebases defensively, right .call(), case hand-crafted envelope reached worker closure never rebased. Verified empirically: un-rebased closure’s original environment (anything bound ) round-trips qs2 wire INTACT (proof carriage path closes); baseenv()-rooted closure instead reconnects RECEIVING session’s baseenv(). Closure-acceptance (primitive explicitly rejected — real formals/body/environment lint rebase); ... prohibited; pkg::fun runs whatever installed child (version claim). Narrowed promise: best-effort static lint rejecting directly detectable unqualified global references — prove behavioral closure, hermetic execution, portability, dependency identity (blind spots: get/mget/assign/string .call, eval(parse()), substitute, .GlobalEnv, formula/attr envs, <<-, S4/R5/R6 dispatch, ambient state). Ad-hoc ; production stays fn_kind=\"package\". Proven closure using get(\"x\", envir = environment()): passes lint (get/environment base), yet fails RUN time real child subprocess rebase severed environment get() otherwise walked. codetools added DESCRIPTION Imports. batch_fn(fn, items, n_workers, dev_path = NULL, collect = FALSE, ...) — IMPLEMENTED: adhoc, return-value sibling batch_run(), transport/worker/-ends validation, R/batch_adhoc.R. Adhoc result identity (§9.4) — IMPLEMENTED: since adhoc envelope carries package/symbol/hash, .batch_inspect_result()/_impl() gained expected_nonce argument; non-NULL replaces package-identity check tgt$fn_kind == \"adhoc\" && tgt$nonce == expected_nonce (id already checked, unconditionally, earlier function). nonce fresh per-item token generator batch_task()’s commit attempt token (.batch_new_attempt_token(), reused, duplicated). batch_task() + adhoc, attempt (commit-record witness, unaffected fn_kind) nonce (adhoc envelope identity) issued — redundant: attempt binds MARKER/commit record; nonce binds ENVELOPE’s executed-code identity, exactly mirroring hash job fn_kind = \"package\". Resolved ambiguity — dev_path adhoc: design text silent dev_path mean “consumer” identity validate . Resolved : dev_path fn_kind = \"adhoc\" ALWAYS names BATCHIT’S source tree (validated via .batch_validate_dev_path(dev_path, \"batchit\"), worker’s runner-load step treats given adhoc dev_path way package-kind runner == consumer self-test branch treats ). lets batchit’s adhoc test suite run source without reinstall; second dev-tree slot helper package closure’s pkg::fun() might reference — resolve via ordinary lazy namespace loading worker regardless (pre-loading step needed, verified: R’s :: operator loads -yet-loaded namespace ). downstream consumer’s batch_fn() calls pass dev_path = NULL practice, getting installed batchit. Resolved: .BATCH_PROTOCOL bump Unit 3. envelope schema change purely additive (fn/nonce new OPTIONAL meta fields); old (pre-Unit-3) worker already rejects fn_kind = \"adhoc\" loudly structurally, code loads (.batch_worker_check()’s hardcoded copy check), existing protocol number already provides version-skew safety net Unit 1’s bump . Confirmed needing touch expect_identical(batchit:::.BATCH_PROTOCOL, 2L) pin test-batch_task.R.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/PHASE6_DESIGN.html","id":"id_6-non-goals","dir":"","previous_headings":"","what":"6. Non-goals","title":"Phase 6′ design (v3) — batchit function dispatch + output-commit","text":"batchit-computed fingerprints, plan records, provenance sidecars, task_scope_id, content hashing, batchit DECIDING skip. §7’s skip CONSUMER-decided; batchit transports record.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/PHASE6_DESIGN.html","id":"id_7-opt-in-consumer-skip--batchit-record--batch_priorbatch_skip-maintainer-approved-2026-07-20-tte-stays-skip-free--removed-2026-07-23","dir":"","previous_headings":"","what":"7. Opt-in consumer skip — .batchit record + batch_prior()/batch_skip() (maintainer-approved 2026-07-20; TTE STAYS SKIP-FREE) — REMOVED 2026-07-23","title":"Phase 6′ design (v3) — batchit function dispatch + output-commit","text":"SUPERSEDED: entire mechanism REMOVED — see PUBLIC_API.md §5. consumer (TTE skip-free doctrine), batch_record()/batch_prior()/batch_skip(), details/skipped fields, prior-marker read deleted; commit engine back pre-skip {protocol, attempt, committed} marker / {committed, attempt} result shape. section retained historical design record Unit 4 built. Build OPT-; production s1/s2/s3 pass skip logic always recompute (Phase 5′ unchanged). - Marker .batchit = commit record + OPTIONAL consumer details value (§0 invariant: opaque batchit; batchit never launches ). - batch_record(details) — fn attaches small serializable details item’s new marker. IMPLEMENTED: exported, R/batch_task.R. Backed .batch_record_env, scoped accessor entered/ exited TIGHTLY around .call() .batch_execute() (mirrors .batch_stage_env exactly). Last call wins (§9.3); errors called outside active batch_task() target run. - batch_prior() — returns PRIOR marker’s details (captured step 0 removal), NULL. fn inspects . IMPLEMENTED: exported, R/batch_task.R. Step 0 .batch_read_prior_marker() (child-side, .call()), reads final marker EXACTLY accepts prior decodes protocol/attempt-token/committed-output-map verify item’s declared outputs (.batch_valid_marker_record()) — malformed, foreign, absent, directory, symlinked marker become prior = NULL. valid prior whose details NULL/absent also makes batch_prior() return NULL (skip disabled), exactly specified §9.2. - batch_skip() — fn RETURNS sentinel → batchit re-commit: verifies prior marker + every declared output still exists (marker vouches fully committed), records done, preserves , writes new marker. Reachable batch_prior() non-NULL. IMPLEMENTED: exported sentinel constructor (errors outside active target run, like two accessors); .batch_execute() detects sentinel (.batch_is_skip()) .call() calls .batch_commit_task_skip() INSTEAD .batch_commit_task() — re-derives witness fresh (re-reads + re-verifies marker, re-stats every declared output regular non-symlink file), never trusting step-0 snapshot. NULL prior, anything longer verifies/exists, fails loud marker left completely untouched (§9.2 point 4). - Doctrine (reconciled §0): batchit READS marker (serve batch_prior / verify commit) HONOURS batch_skip, NEVER decides skip NEVER branches launch marker. details consumer’s — weak details mis-judges currency consumer’s bug, exactly like swereg’s rawbatch/skeleton caches. Safer deleted s1 resume: marker atomic commit point, mtime/existence heuristic. - Commit-record shape change: commit record gained third, ALWAYS-present field — list(committed, attempt, skipped). skipped = FALSE ordinary commit (attempt freshly-dispatched token, unchanged behaviour); skipped = TRUE skip--reuse (attempt PRIOR marker’s token, since nothing new committed — construction can never equal token dispatch just issued). .batch_inspect_result()’s parent-side check skipped-aware: committed output map (names + paths) always checked exactly; attempt-token-must-match- dispatch check applies skipped FALSE. protocol bump (reasoning §5’s -bump note — result-envelope shape produced consumed installed version within one call, dispatch-envelope skew concern).","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/PHASE6_DESIGN.html","id":"id_9-pre-code-specifications-codex-round-3-design-is-implementation-ready-after-these","dir":"","previous_headings":"","what":"9. Pre-code specifications (codex round-3; design is implementation-ready after these)","title":"Phase 6′ design (v3) — batchit function dispatch + output-commit","text":"9.1 Per-item output + marker addressing (P0). - batch_task(fn, items, outputs, style, …): outputs list aligned items — outputs[[]] (named character vector) item ’s output map. Length must equal length(items); items named, outputs may named--item-id instead (set names). - Marker path derived deterministically uniquely per item: file.path(dirname(sort(output_paths)[1]), paste0(\".batchit__\", item_id)) — lexicographically first output’s directory + item’s already-required unique stable id (two items sharing directory never collide). Overridable via explicit per-item marker path consumer needs one. - Marker paths join invocation-wide collision check (§3.1) every output every marker. parent validates marker pathname + parent directory; NEVER inspects marker existence/type/contents launch (child’s job — §0). 9.2 Step-0 / skip state machine (P1, normative). Per item, child: 1. dispatch, .call, read final marker . 2. Accept prior protocol + attempt token + exact output map decode structurally verify; else prior = NULL. Expose prior$details via batch_prior() (NULL valid prior details absent/NULL — valid marker NULL details therefore disables skip; document ). 3. Leave final marker UNTOUCHED function runs. 4. batch_skip(): require non-NULL valid prior, else error (marker removal); re-read + match marker token & output map re-stat every declared output regular non-symlink file; success record done + preserve, writing new marker; mismatch/missing → fail loud, marker untouched. 5. normal return: run commit steps §3.3 1–7 (step 4 removes old marker — old marker survives every new temp ready). 9.3 Skip cleanup + batch_record (P1). Skip successful early exit, must remove every current-attempt stage/temp file reporting reuse. batch_record() called → last call wins. batch_record() batch_skip() → new details discarded, prior marker unchanged. Unserializable/oversized details → NORMAL commit fails old-marker removal. Scoped accessor state (stage dir, prior, record) restored success, error, skip. 9.4 Ad-hoc result identity (P1). parent inspector today requires package/symbol/hash (R/batch.R:465). Result identity now fn_kind: package results bind package+symbol+hash (unchanged); adhoc results bind dispatch attempt token (id + per-dispatch nonce parent issued), since adhoc envelope package identity. .batch_inspect_result branches fn_kind. 9.5 §0 lockdown tests (normative). Prove: parent scheduling/launch traces IDENTICAL absent / valid / malformed / stale markers; marker read occurs worker dispatch; explicit function-returned batch_skip() sentinel enters reuse handling; batchit never compares interprets details. Plus parse/AST guard dispatch-launch path references marker state.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/PHASE6_DESIGN.html","id":"id_8-swereg-migration-separate-phase-after-batchit-ships-no-skip-logic","dir":"","previous_headings":"","what":"8. swereg migration (separate phase, after batchit ships; NO skip logic)","title":"Phase 6′ design (v3) — batchit function dispatch + output-commit","text":"Stage table: s2 FIRST (canary, return, replaces .resume_fresh) → s1c, s1b (return) → s1a, s1d (staged_writer). save_rawbatch later; process_skeletons + s3 scope. : swap hand-rolled write batch_task declared-output call (batch_prior/batch_skip); proven-red (killed mid-commit → marker → item fails loud; wrote-nothing → fails loud); full swereg suite; codex gate; push; CI. Reinstall batchit testing; nothing runs mid-pipeline.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/PUBLIC_API.html","id":null,"dir":"","previous_headings":"","what":"batchit public API","title":"batchit public API","text":"Status: authoritative public surface (naming v2). Adopted design review, 2026-07-22. document defines public functions batchit exports one . supersedes function NAMES used PHASE6_DESIGN.md (batch_run, batch_task, batch_stream, batch_target, batch_fn, batch_stage_path, batch_record, batch_prior, batch_skip). change MECHANISM PHASE6_DESIGN.md — wire protocol, envelope, atomic commit engine, §0 doctrine invariant unchanged. Names bare qualified-use (batchit::run(...)), batch_ prefix — package name already carries . Names full-sentence self-documenting deliberate choice: batchit exports functions wide (CRAN) audience, name states behavior full.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/PUBLIC_API.html","id":"id_1-mental-model","dir":"","previous_headings":"","what":"1. Mental model","title":"batchit public API","text":"batchit brainless muscle: run one function across many argument-sets, one fresh subprocess per set. holds state, decides nothing, knows nothing domain. brain — items run, function, tracking progress — lives consumer (MHT project, swereg’s RegistryStudy R6 class). add stateful “batch” object batchit; shelved provenance program §0 doctrine forbids. item one named argument-set fn — named list whose names match fn’s formals. batchit hands fn arguments checked .call (every element named formal; positional; ...). batchit never looks inside item.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/PUBLIC_API.html","id":"id_2-two-shapes","dir":"","previous_headings":"","what":"2. Two shapes","title":"batchit public API","text":"distinction : can worker read data, must one reader produce ? Shape — normal case. worker opens data. pass complete list items; fn loads computes, running worker. covers every TTE stage: s1, s2, s3, skeleton creation. Shape B — specialist. single sequential source large read independently per worker (55 GB LMED file). One reader — parent — streams hands slices. sole consumer save_rawbatch. Shape norm. Shape B one function one situation. symmetric halves grid; treat Shape B routine choice.","code":""},{"path":[]},{"path":"https://papadopoulos-lab.github.io/batchit/PUBLIC_API.html","id":"id_31-shape-a--the-normal-api","dir":"","previous_headings":"3. The surface","what":"3.1 Shape A — the normal API","title":"batchit public API","text":"One fn loads data computes.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/PUBLIC_API.html","id":"id_32-shape-b--the-specialist","dir":"","previous_headings":"3. The surface","what":"3.2 Shape B — the specialist","title":"batchit public API","text":"parent produces slice via callback; workers commit files atomically. producer callback, function(id) -> item's argument-set. batchit calls parent, lazily, one item time workers free (bounded queue). producer’s RESULT crosses worker; producer function stays parent may access parent environment freely.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/PUBLIC_API.html","id":"id_33-helpers","dir":"","previous_headings":"3. The surface","what":"3.3 Helpers","title":"batchit public API","text":"package_function(package, symbol) — hash-verified descriptor fn. Names package function plus body/formals identity hash, worker refuses code differs parent dispatched. where_to_write_output(name) — called INSIDE style = \"staged_writer\" target; returns exact sibling temp path write output name , streamed write still goes atomic commit.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/PUBLIC_API.html","id":"id_34-fn-a-package-function-or-a-closure","dir":"","previous_headings":"3. The surface","what":"3.4 fn: a package function OR a closure","title":"batchit public API","text":"Every dispatcher’s fn accepts either: package_function(...) descriptor — hash-verified; use production; bare closure — ad-hoc, gated static self-containedness lint mandatory baseenv() rebase; tests one-offs . folds former batch_fn. package-vs-closure choice property fn argument, separate function name.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/PUBLIC_API.html","id":"id_35-batch_envelope-internal-s3-class","dir":"","previous_headings":"3. The surface","what":"3.5 batch_envelope (internal S3 class)","title":"batchit public API","text":"per-item wire unit S3 batch_envelope: structure(list(protocol, meta, args), class = \"batch_envelope\"), validator print method. dispatchers build one envelope per item; child validates . never construct pass envelope. stays plain list wire worker can read one bare qs2:: package loads — trust gate lets worker refuse version-skewed hostile code loading . reason hardcoded structural pre-check inst/batch_worker.R (.batch_worker_check()) stays standalone base-R copy; conformance test asserts agrees S3 validator.","code":""},{"path":[]},{"path":"https://papadopoulos-lab.github.io/batchit/PUBLIC_API.html","id":"id_5-what-is-dropped-and-why","dir":"","previous_headings":"","what":"5. What is dropped, and why","title":"batchit public API","text":"batch_record / batch_prior / batch_skip (opt-consumer skip) — consumer. TTE stages skip-free doctrine (Phase 5’). Re-add real consumer ever needs . batch_fn separate export — package-vs-closure axis fn argument’s type, needs name . Shape B return-value modes (batch_stream collect) — consumer. Shape B collapses single atomic-write specialist.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/PUBLIC_API.html","id":"id_6-semantics-that-changed","dir":"","previous_headings":"","what":"6. Semantics that changed","title":"batchit public API","text":"collect longer flag. run returns nothing; run_and_collect returns list length items, item order, NULL-safe (NULL return keeps slot, vanish). outputs — per-item named character vector, c(<name> = <final path>). Commit atomic: .batchit marker renamed place last commit point. See PHASE6_DESIGN.md section 3. style — \"return\" (target returns list(<name> = <value>) batchit qs2-serializes ) \"staged_writer\" (target writes output where_to_write_output(<name>)). producer runs PARENT; result crosses worker.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/PUBLIC_API.html","id":"id_7-build-plan-separate-phase-codex-gated","dir":"","previous_headings":"","what":"7. Build plan (separate phase, codex-gated)","title":"batchit public API","text":"Rename sweep. Old frontends -> new names; split collect run / run_and_collect; fold batch_fn; delete skip trio; drop Shape B return-value modes. Covers NAMESPACE, roxygen tags, test files, NEWS.md, README.md, PHASE6_DESIGN.md cross-references. Gate: rcmdcheck clean + full test suite green. Build stream_from_parent_and_write_files_atomically. NEW code: Shape B (mirai/producer) transport plus child-side commit engine Shape atomic path already uses (commit engine reads envelope care item arrived). Proven-red test (killed mid-commit -> marker -> item fails loud). codex gate. batch_envelope S3 wrapper — validator + print; conformance test vs hardcoded .batch_worker_check(). (deferred) single-source envelope schema, constructor -package validator derive one declarative field matrix.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/PUBLIC_API.html","id":"id_8-doctrine-unchanged","dir":"","previous_headings":"","what":"8. Doctrine (unchanged)","title":"batchit public API","text":"§0 normative invariant holds: batchit LAUNCH decision depends marker state. caching lives batchit. stateful batch object. See PHASE6_DESIGN.md sections 0 6.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/articles/choosing-a-dispatch-function.html","id":"the-shape-all-four-share","dir":"Articles","previous_headings":"","what":"The shape all four share","title":"Choosing a dispatch function","text":"Three ideas appear every one four functions. fn function run per item. item one call’s arguments, named list. list(x = 2) means “call fn x = 2”. Three four take list items, items. n_workers many items run time. worker process separate R process. run(), run_and_collect() run_and_write_files_atomically() start fresh one every item. stream_from_parent_and_write_files_atomically() instead reuses small pool persistent worker processes (mirai daemons). fresh process per item memory strategy, convenience. R may hold large allocation objects using gone, letting process exit reliable way reclaim . matters one item peaks many gigabytes. Throughout article, parent R session session call batchit .","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/articles/choosing-a-dispatch-function.html","id":"which-one-do-i-want","dir":"Articles","previous_headings":"","what":"Which one do I want?","title":"Choosing a dispatch function","text":"Two things decide . want back? item’s return value; nothing, fn external side effect; output files batchit writes guards . Can hold every item memory ? Usually yes. Say one item’s arguments large data slice, far many items keep one list. Lazy production available combination batchit-managed output files. streaming function hands back raw return values. reaching streaming function, consider simpler alternative: put small identifier file path item fn load large data , inside worker process. keeps fresh-process-per-item memory behaviour, streaming function gives .","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/articles/choosing-a-dispatch-function.html","id":"run_and_collect-a-parallel-lapply","dir":"Articles","previous_headings":"","what":"run_and_collect() — a parallel lapply()","title":"Choosing a dispatch function","text":"Use need every item’s return value. Results come back order items, order worker processes happened finish. returned list never named item id, even items named. two file-writing functions name , asymmetry worth remembering. Every return value travels worker process back parent R session. small object can also travel worker process part item, large one usually better fn read disk .","code":"library(batchit)  squares <- run_and_collect(   fn = function(x) x^2,   items = list(list(x = 2), list(x = 3), list(x = 4)),   n_workers = 2 )  squares #> [[1]] #> [1] 4 #> #> [[2]] #> [1] 9 #> #> [[3]] #> [1] 16"},{"path":"https://papadopoulos-lab.github.io/batchit/articles/choosing-a-dispatch-function.html","id":"run-run-items-and-discard-the-return-values","dir":"Articles","previous_headings":"","what":"run() — run items and discard the return values","title":"Choosing a dispatch function","text":"Use fn called effect outside R: writes files, updates database. works exactly like run_and_collect() returns invisible(NULL). effect external. item runs separate R process, assigning global variable inside fn changes worker process parent R session. Files fn writes , , get protection batchit. fn interrupted partway writing 2.qs2, half-written 2.qs2 left path. run_and_write_files_atomically() prevents .","code":"out_dir <- tempdir()  run(   fn = function(x, dir) qs2::qs_save(x^2, file.path(dir, paste0(x, \".qs2\"))),   items = list(     list(x = 1, dir = out_dir),     list(x = 2, dir = out_dir),     list(x = 3, dir = out_dir)   ),   n_workers = 2 )  qs2::qs_read(file.path(out_dir, \"2.qs2\")) #> [1] 4"},{"path":"https://papadopoulos-lab.github.io/batchit/articles/choosing-a-dispatch-function.html","id":"run_and_write_files_atomically-batchit-writes-the-files","dir":"Articles","previous_headings":"","what":"run_and_write_files_atomically() — batchit writes the files","title":"Choosing a dispatch function","text":"Declare, per item, final path every file item must produce. batchit writes one temporary file directory destination, replaces final files every one item’s outputs written successfully. outputs one named character vector per item, order items — , items named, outputs may carry names instead, order. Every path must absolute, parent directory must already exist, every path across whole call must unique. destination may already exist plain file, case replaced; directory symlink rejected. (Base R portably reject FIFO, socket device file, caught.)","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/articles/choosing-a-dispatch-function.html","id":"style-return-the-default","dir":"Articles","previous_headings":"run_and_write_files_atomically() — batchit writes the files","what":"style = \"return\" (the default)","title":"Choosing a dispatch function","text":"fn returns named list, one element per declared output. names must set declared output names — order, duplicates, none missing extra. batchit saves value path qs2 format. return value fn’s return value. record written, one element per item, order items named item id: committed maps declared output name final path actually written. attempt internal per-item identifier; ignore .","code":"out_dir <- tempdir()  result <- run_and_write_files_atomically(   fn = function(x) list(squared = x^2, doubled = x * 2),   items = list(list(x = 2), list(x = 3)),   outputs = list(     c(squared = file.path(out_dir, \"sq_1.qs2\"),       doubled = file.path(out_dir, \"db_1.qs2\")),     c(squared = file.path(out_dir, \"sq_2.qs2\"),       doubled = file.path(out_dir, \"db_2.qs2\"))   ),   n_workers = 2 )  qs2::qs_read(file.path(out_dir, \"sq_1.qs2\")) #> [1] 4 str(result[[1]]) #> List of 2 #>  $ committed: Named chr [1:2] \"/tmp/.../sq_1.qs2\" \"/tmp/.../db_1.qs2\" #>   ..- attr(*, \"names\")= chr [1:2] \"squared\" \"doubled\" #>  $ attempt  : chr \"...\""},{"path":"https://papadopoulos-lab.github.io/batchit/articles/choosing-a-dispatch-function.html","id":"style-staged_writer-fn-writes-the-files-itself","dir":"Articles","previous_headings":"run_and_write_files_atomically() — batchit writes the files","what":"style = \"staged_writer\" — fn writes the files itself","title":"Choosing a dispatch function","text":"Ask where_to_write_output(name) path write declared output , write . fn’s return value discarded without examined. Discarding return value mean batchit takes word . committing, checks every declared output now exists staging path plain, non-symlink file, errors naming output missing. Two rules where_to_write_output(). Write path returns, leave alone otherwise — batchit moves place . call batchit::where_to_write_output() inline function, inline function may call packages package-qualified form. Pick \"staged_writer\" fn already writes files , format qs2 another package’s writer. writes file directly, instead building whole object memory hand back batchit serialize. Pick \"return\" rest time, time.","code":"write_two_files <- function(x) {   qs2::qs_save(x^2, batchit::where_to_write_output(\"squared\"))   writeLines(as.character(x * 2), batchit::where_to_write_output(\"doubled\"))   invisible(NULL) }  out_dir <- tempdir()  run_and_write_files_atomically(   fn = write_two_files,   items = list(list(x = 2), list(x = 3)),   outputs = list(     c(squared = file.path(out_dir, \"sq_1.qs2\"),       doubled = file.path(out_dir, \"db_1.txt\")),     c(squared = file.path(out_dir, \"sq_2.qs2\"),       doubled = file.path(out_dir, \"db_2.txt\"))   ),   style = \"staged_writer\",   n_workers = 2 )  readLines(file.path(out_dir, \"db_1.txt\")) #> [1] \"4\""},{"path":"https://papadopoulos-lab.github.io/batchit/articles/choosing-a-dispatch-function.html","id":"stream_from_parent_and_write_files_atomically-build-items-lazily","dir":"Articles","previous_headings":"","what":"stream_from_parent_and_write_files_atomically() — build items lazily","title":"Choosing a dispatch function","text":"Use building full items list use much memory. Instead items, give ids vector producer(id) function builds one item. batchit keeps min(2 * n_workers, length(ids)) items flight, calls producer() one slots free. Note slot idle worker process: every worker process busy, roughly n_workers items can already produced queued. producer() runs parent R session, never worker process, ordinary function — self-containedness rules apply fn, . Load build item’s data inside producer(); beforehand defeats point. producer() returns item, batchit applies outputs validation, two styles commit record run_and_write_files_atomically(). Three differences three functions. fn must come package_function(). inline function accepted. needs mirai package: pak::pak(\"mirai\"). worker processes persistent mirai daemons. worker process’s memory reclaimed items way process exit reclaims elsewhere, suits many small--medium items better huge ones. daemons live private compute profile batchit creates tears one call. never touches mirai daemon configuration set .","code":"# `write_one_slice()` must live in an INSTALLED package: this function loads it # by package name and function name, never by value. Put it in your own # package's R/ directory, install, and replace \"yourpkg\" below. # #   write_one_slice <- function(slice) list(main = slice)  out_dir <- tempdir() ids <- c(\"a\", \"b\", \"c\")  stream_from_parent_and_write_files_atomically(   fn = package_function(\"yourpkg\", \"write_one_slice\"),   ids = ids,   # Replace this with your own loader; it runs in the parent R session.   producer = function(id) list(slice = toupper(id)),   outputs = setNames(     lapply(ids, function(id) c(main = file.path(out_dir, paste0(id, \".qs2\")))),     ids   ),   n_workers = 2 )"},{"path":"https://papadopoulos-lab.github.io/batchit/articles/choosing-a-dispatch-function.html","id":"naming-the-function-inline-or-package_function","dir":"Articles","previous_headings":"","what":"Naming the function: inline or package_function()","title":"Choosing a dispatch function","text":"inline function ordinary R function passed value — written directly call, assigned variable first, write_two_files . quick option, every example except streaming one uses . must self-contained: may use arguments, base R, pkg::fun()-qualified calls packages. batchit checks best-effort static scan, proof. Indirect lookups get(), eval(parse()) string-based .call() can pass check still fail worker process, function’s environment replaced sent. Keep rule even check lets something . fn needs something outside arguments, pass another argument, reference pkg::fun(), move package. package_function(package, symbol) names function installed package instead. records hash function’s code, worker process refuses run definition loads match. Use production runs. hash deliberately narrow. covers target function’s body formals, nothing else — changed helper calls, changed constant, different dependency version outside . match proves body formals definition parent R session hashed, behaviour identical. ignores comments whitespace, installed package devtools::load_all() tree agree identical code. symbol may name internal function; exported.","code":"# Allowed -- own argument and base R: function(x) x^2  # Allowed -- another package, package-qualified: function(path) qs2::qs_read(path)  # NOT allowed -- `threshold` is not an argument of this function: threshold <- 10 function(x) x > threshold  # NOT allowed -- `my_helper` is a plain call to a function defined outside: my_helper <- function(x) x * 2 function(x) my_helper(x) fn <- package_function(\"yourpkg\", \"fit_one_model\") fn$formal_names #> [1] \"id\" \"n_iter\""},{"path":"https://papadopoulos-lab.github.io/batchit/articles/choosing-a-dispatch-function.html","id":"name-every-argument-every-time","dir":"Articles","previous_headings":"","what":"Name every argument, every time","title":"Choosing a dispatch function","text":"every item, batchit checks named every formal fn — including ones default — nothing else. checks parent R session dispatching, worker process. Requiring defaults named makes forgotten argument visible. argument meant set left otherwise looks exactly like one deliberately left default. reason, fn must take ....","code":"fn <- function(x, scale = 1) x * scale  # Rejected -- `scale` has a default, but it still must be named: items <- list(list(x = 2))  # Correct: items <- list(list(x = 2, scale = 1))"},{"path":"https://papadopoulos-lab.github.io/batchit/articles/choosing-a-dispatch-function.html","id":"what-the-atomic-write-guarantee-covers","dir":"Articles","previous_headings":"","what":"What the atomic-write guarantee covers","title":"Choosing a dispatch function","text":"batchit stages every one item’s declared outputs replaces final file. replaces final files one time, rename within destination directory. Last, writes marker recording commit. guarantee different three phases. first replacement — fully covered. item fails — error fn, \"return\" value whose names match declared outputs, declared output \"staged_writer\" never wrote, failure serializing one staged files — declared output touched. Whatever paths stays exactly , nothing , nothing appears. item’s previous marker removed first replacement, one survive even . replacement loop — transactional. worker process killed, rename fails, partway loop, item’s outputs can new others still old. batchit never rolls back file already renamed. individual file left half-written — rename replacement atomic filesystem semantics batchit supports — set can torn. marker written — committed, call may still report failure. worker serializes result envelope committing. fails, worker killed window, every final file marker correctly place call reports item failed. timeout confined one phase. timeout measures worker process’s whole lifetime, can land three — batchit’s source notes timeout can kill worker mid-commit. read “timeout” pre-replacement failure. marker tells complete commit torn one: small file named .batchit__<item id>, written every replacement succeeded. torn set valid marker vouching . batchit places one per item, directory item’s lexicographically first declared output path — beside outputs share directory, one . create, read depend file ; batchit manages . name built item id, id used either file-writing function must contain / \\. skip mechanism. batchit never treats existing output file reason skip item. schedules every item, subject validation earlier failure stopping call first. Per item, per call. one item fails call stops, items already committed keep files. batchit undo .","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/articles/choosing-a-dispatch-function.html","id":"when-something-fails","dir":"Articles","previous_headings":"","what":"When something fails","title":"Choosing a dispatch function","text":"item’s worker process raises error, exits unexpectedly, runs past timeout, call stops R error. continue past failure, never returns partial result error object failed slot. Work already running may finish call unwinds. run(), run_and_collect() run_and_write_files_atomically() print failing worker process’s combined stdout stderr first — last 64,000 bytes, 100 lines — cause visible. stream_from_parent_and_write_files_atomically() reports error came back mirai; equivalent captured-log tail. success, worker process’s stdout stderr discarded. Warnings exception: first 100 warnings per item carried back re-raised parent R session, labelled item id. timeout 6 hours default, per item. Pass Inf disable . Item ids come names items, ids streaming, label progress error messages. Name items, message reads item 'fit_03' exceeded timeout rather item '3'.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/articles/choosing-a-dispatch-function.html","id":"two-settings-worth-knowing","dir":"Articles","previous_headings":"","what":"Two settings worth knowing","title":"Choosing a dispatch function","text":"dev_path loads package source tree worker process devtools::load_all(), instead using installed package. tree must name depends fn: package_function() target, package’s source tree. Load tree parent R session building descriptor, hash parent records matches code worker process loads. inline function, consumer package load, dev_path can name batchit’s source tree. useful developing batchit . Leave NULL use installed packages. path exist, hold expected package, error — never falls back installed version quietly. Thread counts. batchit set BLAS data.table thread counts . fn multi-threaded, lower thread count running many worker processes, compete cores.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/articles/choosing-a-dispatch-function.html","id":"see-also","dir":"Articles","previous_headings":"","what":"See also","title":"Choosing a dispatch function","text":"?run, ?run_and_collect, ?run_and_write_files_atomically, ?stream_from_parent_and_write_files_atomically — full argument reference. ?package_function, ?where_to_write_output — two helpers.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/authors.html","id":null,"dir":"","previous_headings":"","what":"Authors","title":"Authors and Citation","text":"package proudly brought : Richard Aubrey White. Author, maintainer. See changelog contributors.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/authors.html","id":"citation","dir":"","previous_headings":"","what":"Citation","title":"Authors and Citation","text":"White RA (2026). batchit: One Subprocess Dispatcher -Ends-Validated Contract. R package version 26.7.29.1, https://papadopoulos-lab.github.io/batchit.","code":"@Manual{,   title = {batchit: One Subprocess Dispatcher with a Both-Ends-Validated Contract},   author = {Richard Aubrey White},   year = {2026},   note = {R package version 26.7.29.1},   url = {https://papadopoulos-lab.github.io/batchit}, }"},{"path":"https://papadopoulos-lab.github.io/batchit/index.html","id":"batchit","dir":"","previous_headings":"","what":"batchit","title":"One Subprocess Dispatcher with a Both-Ends-Validated Contract","text":"batchit runs one R function many times parallel, call worker process, either gives return values back writes call’s output files .","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/index.html","id":"quick-start","dir":"","previous_headings":"","what":"Quick start","title":"One Subprocess Dispatcher with a Both-Ends-Validated Contract","text":"Three things happened: element items one call’s arguments, named list. list(x = 2) means “call fn x = 2”. call ran , brand-new R process (worker). n_workers (, 2) calls ran time. results came back list, order items – order workers happened finish.","code":"library(batchit)  squares <- run_and_collect(   fn = function(x) x^2,   items = list(list(x = 2), list(x = 3), list(x = 4)),   n_workers = 2 )  squares #> [[1]] #> [1] 4 #> #> [[2]] #> [1] 9 #> #> [[3]] #> [1] 16"},{"path":"https://papadopoulos-lab.github.io/batchit/index.html","id":"which-function-do-i-want","dir":"","previous_headings":"","what":"Which function do I want?","title":"One Subprocess Dispatcher with a Both-Ends-Validated Contract","text":"run(), run_and_collect(), run_and_write_files_atomically() work way quick start : build full items list first, item runs fresh worker process. fresh process per item matters memory-heavy work – worker’s R session exits, operating system reclaims memory used, true worker reused next item. stream_from_parent_and_write_files_atomically() building full items list first use much memory (example: item large data slice, far many items hold list). Instead, give ids vector function produces one item’s arguments time. batchit keeps min(2 * n_workers, length(ids)) items flight calls function one slots free, bounds many produced items exist . trade-: function uses small number long-running background workers instead one fresh process per item, single worker’s memory automatically reclaimed items way process exit reclaims three functions. ’s better fit many small--medium items huge ones.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/index.html","id":"writing-the-function-you-dispatch-fn","dir":"","previous_headings":"","what":"Writing the function you dispatch (fn)","title":"One Subprocess Dispatcher with a Both-Ends-Validated Contract","text":"Every one four functions takes fn argument: function run per item. can pass two ways: inline function – ordinary R function passed value, whether written directly call (quick start ) assigned variable first. Supported run(), run_and_collect(), run_and_write_files_atomically(). supported stream_from_parent_and_write_files_atomically(), requires second form . reference built package_function(package, symbol), naming function already lives installed R package (see ?package_function). Use production runs, want every worker verify running exact code tested, rather whatever happens installed. Required stream_from_parent_and_write_files_atomically(); optional (recommended) three.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/index.html","id":"rules-for-an-inline-function","dir":"","previous_headings":"Writing the function you dispatch (fn)","what":"Rules for an inline function","title":"One Subprocess Dispatcher with a Both-Ends-Validated Contract","text":"inline fn must self-contained: may use (1) arguments, (2) base R functions operators, (3) packages’ functions called explicit pkg::fun() prefix. must take .... batchit checks best-effort static scan, proof. Indirect lookups get(), eval(parse()) string-based .call() can pass check still fail worker, function’s environment replaced sent. Keep rule even check lets something . function needs something outside arguments, either pass extra argument (add every item), reference pkg::fun() lives installed package, move function package use package_function() instead.","code":"# Allowed -- uses only its own argument and base R: function(x) x^2  # Allowed -- calls another package's function, package-qualified: function(x) data.table::data.table(x = x, y = x^2)  # NOT allowed -- `threshold` is not an argument of this function: threshold <- 10 function(x) x > threshold  # NOT allowed -- `my_helper` is a plain (non-package-qualified) call to a # function defined outside this one: my_helper <- function(x) x * 2 function(x) my_helper(x)"},{"path":"https://papadopoulos-lab.github.io/batchit/index.html","id":"writing-output-files-safely","dir":"","previous_headings":"","what":"Writing output files safely","title":"One Subprocess Dispatcher with a Both-Ends-Validated Contract","text":"run_and_write_files_atomically() stream_from_parent_and_write_files_atomically() give guarantee files item produces: failed interrupted item never leaves half-written file final path. batchit stages every one item’s declared outputs – temporary file destination’s directory – replaces final file. replaces final files one time, rename, writes commit marker last. guarantee differs across three phases. first replacement – error fn, missing misnamed output, failure serializing staged file – declared output touched, whatever already (nothing, didn’t exist yet) left untouched. replacement loop, set transactional. kill rename failure partway can leave item’s outputs replaced others ; batchit never rolls back final already renamed. individual file left half-written – rename replacement atomic filesystem semantics batchit supports – set can torn, torn set valid commit marker vouching . marker written, files committed even call still reports failure: worker serializes result envelope committing, failure window leaves everything correctly place item reported failed. timeout confined one phase. measures worker’s whole lifetime can land three, including mid-commit. batchit never treats existing output file reason skip item. schedules every item, subject validation earlier failure stopping call first. tell functions write outputs argument: one absolute final path per declared output name, per item, e.g. c(main = \"/data/result.qs2\"). two ways fn actually produce files, chosen style argument: style = \"return\" (default): fn returns named list, one element per declared output, batchit saves value file (qs2 format – read back qs2::qs_read()). style = \"staged_writer\": fn writes output , path given where_to_write_output(<name>); return value ignored. Use fn already writes files (example, format qs2, via another package’s writer function), doesn’t build whole object memory just hand back batchit save . functions return list, one element per item, describing written – never fn’s raw return value. See ?run_and_write_files_atomically ?stream_from_parent_and_write_files_atomically complete, runnable examples styles.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/index.html","id":"what-you-see-when-something-goes-wrong","dir":"","previous_headings":"","what":"What you see when something goes wrong","title":"One Subprocess Dispatcher with a Both-Ends-Validated Contract","text":"item’s worker errors, exits unexpectedly, runs longer timeout seconds (6 hours default), call stops R error – continue past failure return partial result. Work already running may finish call unwinds. run(), run_and_collect() run_and_write_files_atomically() print failing worker’s combined stdout stderr first – last 64,000 bytes, 100 lines – can see happened. stream_from_parent_and_write_files_atomically() reports error came back mirai; equivalent captured-log tail. success worker’s output discarded. Warnings exception: first 100 per item carried back re-raised session, labelled item id.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/index.html","id":"installation","dir":"","previous_headings":"","what":"Installation","title":"One Subprocess Dispatcher with a Both-Ends-Validated Contract","text":"stream_from_parent_and_write_files_atomically() additionally needs mirai package:","code":"pak::pak(\"papadopoulos-lab/batchit\") pak::pak(\"mirai\")"},{"path":"https://papadopoulos-lab.github.io/batchit/index.html","id":"advanced","dir":"","previous_headings":"","what":"Advanced","title":"One Subprocess Dispatcher with a Both-Ends-Validated Contract","text":"notes explain batchit works internally. don’t need use package – matter ’re debugging something unusual, deciding whether use package_function() production. Code-identity check. fn package_function() reference, worker re-loads function computes hash body arguments, refuses run hash doesn’t match R session computed called package_function(). catches worker silently running older different version code (example, installed package date compared development version tested ). hash covers target function’s body arguments – changed helper function calls, changed constant refers , different dependency version covered, proves “function definition”, “provably identical behaviour”. hash also ignores comments whitespace, agrees whether function loaded installed package devtools::load_all(). Argument-name checking. every item, batchit checks named every one fn’s arguments – including ones default value – named nothing else, R session (dispatching) inside worker (running). Requiring every argument name, defaults included, catches argument meant set silently left ; missing--optional argument otherwise look identical one deliberately left default. dev_path. Every dispatch function takes dev_path argument: package source tree loaded worker devtools::load_all() instead installed package. tree must name depends fn. package_function() reference package’s source tree, load tree session building reference, hash parent records matches worker loads. inline function consumer package load, dev_path can name batchit’s source tree – useful developing batchit . Leave NULL (default) use installed package. path doesn’t exist, doesn’t match expected package, error – never silently falls back installed version. Thread settings. batchit set BLAS data.table thread counts . fn multi-threaded, reduce thread count running several workers , workers don’t compete CPU cores (oversubscription). internal marker file. item commits output files successfully, batchit leaves small bookkeeping file named .batchit__<item id>, record specific run finished writing every declared output. one marker per item, directory item’s lexicographically first declared output path – beside outputs share directory, one . Don’t create, read, rely file name – batchit manages entirely, overwrites cleanly next time item id committed. Item ids (items’ names, ids streaming function) must contain / \\, since filename built directly . stream_from_parent_and_write_files_atomically()’s background workers. function runs workers persistent mirai daemons, private compute profile creates tears one call – never touches resets mirai daemon configuration already . min(2 * n_workers, length(ids)) items flight , function produces next item’s arguments called one slots free – bounds many produced items exist time. free slot idle worker: every worker busy, roughly n_workers items may already produced queued. bound covers produced item arguments, ids, outputs result list, live session whole call.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/index.html","id":"provenance","dir":"","previous_headings":"","what":"Provenance","title":"One Subprocess Dispatcher with a Both-Ends-Validated Contract","text":"batchit began part swereg package, dispatch logic ran registry-analysis pipelines production. later extracted package projects use without depending swereg. See swereg’s PROJECT.md history.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/reference/batchit-package.html","id":null,"dir":"Reference","previous_headings":"","what":"batchit: run one R function across many parallel worker processes — batchit-package","title":"batchit: run one R function across many parallel worker processes — batchit-package","text":"batchit runs one R function per item list inputs, call worker process, either collects return values worker write output files . Pick one four functions, depending need:","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/reference/batchit-package.html","id":"details","dir":"Reference","previous_headings":"","what":"Details","title":"batchit: run one R function across many parallel worker processes — batchit-package","text":"item named list holding arguments one call function (every argument must named, including ones default value). function can either inline function write directly call, reference function installed package built package_function() – see function's help page prefer one . runnable quick-start example fuller explanation function, see README https://github.com/papadopoulos-lab/batchit.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/reference/batchit-package.html","id":"advanced","dir":"Reference","previous_headings":"","what":"Advanced","title":"batchit: run one R function across many parallel worker processes — batchit-package","text":"run(), run_and_collect(), run_and_write_files_atomically() run items fresh processx subprocess, one per item. stream_from_parent_and_write_files_atomically() instead runs items persistent mirai background workers, items can produced lazily, bounded amount -flight memory, instead built list front. fn package_function() reference, batchit checks worker loaded exact function definition R session (via hash function's body arguments), validates every item's argument names function's arguments – R session worker, anything runs. batchit change BLAS data.table thread settings; function multi-threaded, divide CPU cores across n_workers .","code":""},{"path":[]},{"path":"https://papadopoulos-lab.github.io/batchit/reference/batchit-package.html","id":"author","dir":"Reference","previous_headings":"","what":"Author","title":"batchit: run one R function across many parallel worker processes — batchit-package","text":"Maintainer: Richard Aubrey White hello@rwhite.(ORCID) Authors: Richard Aubrey White hello@rwhite.(ORCID)","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/reference/package_function.html","id":null,"dir":"Reference","previous_headings":"","what":"Identify a function in an installed package, so a worker can run it — package_function","title":"Identify a function in an installed package, so a worker can run it — package_function","text":"Builds small object names function installed package, passing fn argument run(), run_and_collect(), run_and_write_files_atomically(), stream_from_parent_and_write_files_atomically(). alternative writing inline function directly one calls (see help pages) – package_function() required stream_from_parent_and_write_files_atomically(), recommended others whenever want production run verify every worker really running code tested, just whatever happens installed. quick one-run, inline function simpler needs setup.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/reference/package_function.html","id":"ref-usage","dir":"Reference","previous_headings":"","what":"Usage","title":"Identify a function in an installed package, so a worker can run it — package_function","text":"","code":"package_function(package, symbol, version = NULL)"},{"path":"https://papadopoulos-lab.github.io/batchit/reference/package_function.html","id":"arguments","dir":"Reference","previous_headings":"","what":"Arguments","title":"Identify a function in an installed package, so a worker can run it — package_function","text":"package Name installed package holding function (single string). need batchit . symbol Name function inside package (single string). can exported internal (unexported) function name. version version label record reference. Defaults package's currently installed version. informational : worker actually checks running code hash , version string.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/reference/package_function.html","id":"value","dir":"Reference","previous_headings":"","what":"Value","title":"Identify a function in an installed package, so a worker can run it — package_function","text":"object class \"package_function\": list elements package, symbol, version, hash (hash function's code, used verify worker loaded definition), formal_names (function's argument names). Pass whole object fn.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/reference/package_function.html","id":"details","dir":"Reference","previous_headings":"","what":"Details","title":"Identify a function in an installed package, so a worker can run it — package_function","text":"object function returns always identifies function package name + function name + hash code – never function , bare function name, closure. different passing function directly fn, run(), run_and_collect(), run_and_write_files_atomically() also accept (see fn argument); stream_from_parent_and_write_files_atomically() requires form built function. function takes ... rejected: batchit checks every item's argument names function's fixed argument list, ... make mistyped missing argument impossible catch reliably.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/reference/package_function.html","id":"advanced","dir":"Reference","previous_headings":"","what":"Advanced","title":"Identify a function in an installed package, so a worker can run it — package_function","text":"code hash deliberately narrow: covers function's body argument list. changed helper function calls, constant refers elsewhere, S4/R6 method table, dependency's version outside – matching hash proves \"function definition\", \"provably identical behaviour\". hash also computed stripping comments whitespace (utils::removeSource()), agrees whether function loaded installed package devtools::load_all() source tree – otherwise disagree identical code.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/reference/package_function.html","id":"ref-examples","dir":"Reference","previous_headings":"","what":"Examples","title":"Identify a function in an installed package, so a worker can run it — package_function","text":"","code":"if (FALSE) { # \\dontrun{ # `stats` ships with R, so this always works. In your own project, name # your own package and function here instead. t <- package_function(\"stats\", \"sd\") t$formal_names } # }"},{"path":"https://papadopoulos-lab.github.io/batchit/reference/run.html","id":null,"dir":"Reference","previous_headings":"","what":"Run a function once per item, in a fresh worker process, discarding the results — run","title":"Run a function once per item, in a fresh worker process, discarding the results — run","text":"Use parallel loop: fn runs per item, call , brand-new R process (worker), n_workers running time. Use specifically need anything back R session – example, fn writes files, called purely side effect. want call's return value back, use run_and_collect() instead; works identically otherwise. want batchit manage output files safely (failed item never leaves half-written file), use run_and_write_files_atomically() instead – files fn writes get none protection: fn interrupted partway writing one, whatever already wrote left exactly .","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/reference/run.html","id":"ref-usage","dir":"Reference","previous_headings":"","what":"Usage","title":"Run a function once per item, in a fresh worker process, discarding the results — run","text":"","code":"run(   fn,   items,   n_workers,   dev_path = NULL,   p = NULL,   label = NULL,   timeout = .BATCH_DEFAULT_TIMEOUT )"},{"path":"https://papadopoulos-lab.github.io/batchit/reference/run.html","id":"arguments","dir":"Reference","previous_headings":"","what":"Arguments","title":"Run a function once per item, in a fresh worker process, discarding the results — run","text":"fn function run per item. Either inline function written directly call, object package_function() naming function installed package. inline function must self-contained: may use arguments, base R functions/operators, pkg::fun()-qualified calls packages – see Advanced section accepted rejected examples. items One entry per call. entry named list holding arguments one call fn – every argument fn takes must named, including ones default value (omitted optional argument treated mistake, \"use default\", silently dropped argument caught rather passed unnoticed). named entry keeps name item's id (used progress messages error messages); unnamed entry identified position instead (1, 2, 3, ...). n_workers many items run time (whole number, 1 ). dev_path Advanced; see Advanced section . Leave NULL (default) run installed version package. p progress callback, progressr progressor. Leave NULL (default) print simple progress messages instead. label optional short label added progress message (example, stage name). timeout Maximum time, seconds, let one item's worker run killing reporting failed. Defaults 6 hours; pass Inf disable limit.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/reference/run.html","id":"value","dir":"Reference","previous_headings":"","what":"Value","title":"Run a function once per item, in a fresh worker process, discarding the results — run","text":"Nothing useful (invisible(NULL)). Use run_and_collect() need item's return value.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/reference/run.html","id":"details","dir":"Reference","previous_headings":"","what":"Details","title":"Run a function once per item, in a fresh worker process, discarding the results — run","text":"item's worker errors, exits unexpectedly, exceeds timeout, whole call stops immediately R error (printing worker's captured output first) – continue past failure.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/reference/run.html","id":"advanced","dir":"Reference","previous_headings":"","what":"Advanced","title":"Run a function once per item, in a fresh worker process, discarding the results — run","text":"Accepted rejected inline functions:   fn package_function() reference, worker re-checks hash code running , refuses run code changed since called package_function() – see function's help page hash cover. dev_path names package source tree load worker devtools::load_all(), instead using installed package – package named package_function() reference, (inline fn) batchit's source tree. path exist, match expected package, error rather silent fall-back installed version. batchit set BLAS data.table thread counts. fn multi-threaded, reduce thread count running several workers , avoid oversubscribing CPU cores.","code":"# Allowed -- uses only its own argument and base R: function(x) x^2  # Allowed -- calls another package's function, package-qualified: function(x) data.table::data.table(x = x, y = x^2)  # NOT allowed -- `threshold` is not an argument of this function: threshold <- 10 function(x) x > threshold  # NOT allowed -- `my_helper` is a plain call to a function defined # outside this one: my_helper <- function(x) x * 2 function(x) my_helper(x)"},{"path":"https://papadopoulos-lab.github.io/batchit/reference/run.html","id":"ref-examples","dir":"Reference","previous_headings":"","what":"Examples","title":"Run a function once per item, in a fresh worker process, discarding the results — run","text":"","code":"if (FALSE) { # \\dontrun{ out_dir <- tempdir() run(   fn = function(x, dir) saveRDS(x^2, file.path(dir, paste0(x, \".rds\"))),   items = list(     list(x = 1, dir = out_dir),     list(x = 2, dir = out_dir),     list(x = 3, dir = out_dir)   ),   n_workers = 2 ) list.files(out_dir) readRDS(file.path(out_dir, \"2.rds\")) # 4 } # }"},{"path":"https://papadopoulos-lab.github.io/batchit/reference/run_and_collect.html","id":null,"dir":"Reference","previous_headings":"","what":"Run a function once per item, in a fresh worker process, and collect the results — run_and_collect","title":"Run a function once per item, in a fresh worker process, and collect the results — run_and_collect","text":"Use parallel version lapply(): fn runs per item, call , brand-new R process (worker), n_workers running time, get back list call's return value. need return values – fn writes output, called side effect – use run() instead; works identically discards .","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/reference/run_and_collect.html","id":"ref-usage","dir":"Reference","previous_headings":"","what":"Usage","title":"Run a function once per item, in a fresh worker process, and collect the results — run_and_collect","text":"","code":"run_and_collect(   fn,   items,   n_workers,   dev_path = NULL,   p = NULL,   label = NULL,   timeout = .BATCH_DEFAULT_TIMEOUT )"},{"path":"https://papadopoulos-lab.github.io/batchit/reference/run_and_collect.html","id":"arguments","dir":"Reference","previous_headings":"","what":"Arguments","title":"Run a function once per item, in a fresh worker process, and collect the results — run_and_collect","text":"fn function run per item. Either inline function written directly call, object package_function() naming function installed package. inline function must self-contained: may use arguments, base R functions/operators, pkg::fun()-qualified calls packages – see run()'s Advanced section accepted rejected examples. items One entry per call. entry named list holding arguments one call fn – every argument fn takes must named, including ones default value (omitted optional argument treated mistake, \"use default\", silently dropped argument caught rather passed unnoticed). named entry keeps name item's id (used progress messages error messages); unnamed entry identified position instead (1, 2, 3, ...). n_workers many items run time (whole number, 1 ). dev_path Advanced; see run()'s Advanced section. Leave NULL (default) run installed version package. p progress callback, progressr progressor. Leave NULL (default) print simple progress messages instead. label optional short label added progress message (example, stage name). timeout Maximum time, seconds, let one item's worker run killing reporting failed. Defaults 6 hours; pass Inf disable limit.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/reference/run_and_collect.html","id":"value","dir":"Reference","previous_headings":"","what":"Value","title":"Run a function once per item, in a fresh worker process, and collect the results — run_and_collect","text":"list item's return value, one element per item, order items (order workers happened finish). list never named item id – even items named – unlike run_and_write_files_atomically() stream_from_parent_and_write_files_atomically(), whose results .","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/reference/run_and_collect.html","id":"details","dir":"Reference","previous_headings":"","what":"Details","title":"Run a function once per item, in a fresh worker process, and collect the results — run_and_collect","text":"small object can included directly item's arguments (travels worker rest item), large object usually better fn load inside worker (example, read disk) rather pass items. item's worker errors, exits unexpectedly, exceeds timeout, whole call stops immediately R error (printing worker's captured output first) – never returns partial list, never puts error object failed item's slot.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/reference/run_and_collect.html","id":"advanced","dir":"Reference","previous_headings":"","what":"Advanced","title":"Run a function once per item, in a fresh worker process, and collect the results — run_and_collect","text":"Fresh worker processes just convenience – memory strategy memory-heavy work. one item's analysis peaks , say, tens gigabytes, R hand memory back operating system ; exiting worker process reclaims . batchit starts new worker per item instead reusing one across items. fn package_function() reference, worker re-checks hash code running , refuses run code changed since called package_function(). warning fn raises captured re-raised R session item finishes, labelled item id. batchit set BLAS data.table thread counts. fn multi-threaded, reduce thread count running several workers , avoid oversubscribing CPU cores.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/reference/run_and_collect.html","id":"ref-examples","dir":"Reference","previous_headings":"","what":"Examples","title":"Run a function once per item, in a fresh worker process, and collect the results — run_and_collect","text":"","code":"if (FALSE) { # \\dontrun{ squares <- run_and_collect(   fn = function(x) x^2,   items = list(list(x = 2), list(x = 3), list(x = 4)),   n_workers = 2 ) squares } # }"},{"path":"https://papadopoulos-lab.github.io/batchit/reference/run_and_write_files_atomically.html","id":null,"dir":"Reference","previous_headings":"","what":"Run a function once per item, in a fresh worker process, and have batchit write its output files safely — run_and_write_files_atomically","title":"Run a function once per item, in a fresh worker process, and have batchit write its output files safely — run_and_write_files_atomically","text":"Use item's job produce one files, want guarantee failed interrupted item never leaves half-written file final path. item runs fn , brand-new R process (worker), n_workers running time – like run() run_and_collect() – instead fn's return value crossing back , batchit writes files declared outputs hands back small record wrote.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/reference/run_and_write_files_atomically.html","id":"ref-usage","dir":"Reference","previous_headings":"","what":"Usage","title":"Run a function once per item, in a fresh worker process, and have batchit write its output files safely — run_and_write_files_atomically","text":"","code":"run_and_write_files_atomically(   fn,   items,   outputs,   style = \"return\",   n_workers,   dev_path = NULL,   p = NULL,   label = NULL,   timeout = .BATCH_DEFAULT_TIMEOUT,   target = NULL )"},{"path":"https://papadopoulos-lab.github.io/batchit/reference/run_and_write_files_atomically.html","id":"arguments","dir":"Reference","previous_headings":"","what":"Arguments","title":"Run a function once per item, in a fresh worker process, and have batchit write its output files safely — run_and_write_files_atomically","text":"fn function run per item. Either inline function written directly call, object package_function() naming function installed package. inline function must self-contained: may use arguments, base R functions/operators, pkg::fun()-qualified calls packages (including call where_to_write_output(), must written batchit::where_to_write_output()) – see run()'s Advanced section accepted rejected examples. items One entry per call. entry named list holding arguments one call fn – every argument fn takes must named, including ones default value (omitted optional argument treated mistake, \"use default\"). named entry keeps name item's id; unnamed entry identified position instead (1, 2, 3, ...). Item ids must contain / \\ (used build internal bookkeeping filename – see Advanced section package README). outputs files fn must produce item. One element per item, order items (, items named, may instead name outputs way – names, order). element named character vector giving FINAL path declared output, e.g. c(main = \"/path//result.qs2\"). Every path must absolute; every destination must either exist yet, already ordinary file (directory symlink); every path, across every item one call, must unique. style \"return\" (default) \"staged_writer\" – see description means fn. value errors. n_workers many items run time (whole number, 1 ). dev_path Advanced; see run()'s Advanced section. Leave NULL (default) run installed version package. p progress callback, progressr progressor. label optional short label added progress message. timeout Maximum time, seconds, let one item's worker run killing reporting failed; see run(). target Deprecated former name fn, kept old callers. Pass fn instead; supplying error.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/reference/run_and_write_files_atomically.html","id":"value","dir":"Reference","previous_headings":"","what":"Value","title":"Run a function once per item, in a fresh worker process, and have batchit write its output files safely — run_and_write_files_atomically","text":"list, one element per item, order items, named item id. element describes written – list(committed = <named character vector: output name -> final path actually written>, attempt = <internal per-item identifier; can ignore >). Never fn's raw return value.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/reference/run_and_write_files_atomically.html","id":"details","dir":"Reference","previous_headings":"","what":"Details","title":"Run a function once per item, in a fresh worker process, and have batchit write its output files safely — run_and_write_files_atomically","text":"guarantee, boundary. batchit stages every one item's declared outputs – temporary file final destination's directory – replaces final file. replaces final files one time, rename, writes commit marker last. guarantee differs across three phases. FIRST REPLACEMENT – fully covered. error fn, \"return\" value whose names match declared outputs, declared output \"staged_writer\" never wrote, failure serializing staged file – case declared output touched, whatever already paths (nothing, exist) left untouched. item's previous marker removed first replacement, survive even . REPLACEMENT LOOP – transactional. kill rename failure partway can leave item's outputs replaced others ; batchit never rolls back final already renamed. individual file left half-written – rename replacement atomic filesystem semantics batchit supports – SET can torn, torn set valid marker vouching . MARKER WRITTEN – committed, even call reports failure. worker serializes result envelope committing, failure window leaves every final marker correctly place item reported failed. timeout confined one phase: measures worker's whole lifetime can land three, including mid-commit. batchit never treats existing output file reason skip item; schedules every item, subject validation earlier failure stopping call first. two ways fn produce declared outputs, chosen style: style = \"return\" (default): fn returns named list, one element per declared output, names matching outputs exactly; batchit saves value file (qs2 format – read back qs2::qs_read()). style = \"staged_writer\": fn writes output , path given where_to_write_output()(<name>); return value ignored. Use fn already writes files (example, format qs2, via another package's writer), build whole object memory just hand back batchit save . Use run() run_and_collect() instead happy managing output files without guarantee, want value back R rather files disk. Use stream_from_parent_and_write_files_atomically() instead building every item front (plain list, way items works ) use much memory.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/reference/run_and_write_files_atomically.html","id":"advanced","dir":"Reference","previous_headings":"","what":"Advanced","title":"Run a function once per item, in a fresh worker process, and have batchit write its output files safely — run_and_write_files_atomically","text":"Uses fresh-worker-per-item transport run() run_and_collect() (one processx subprocess per item), code-identity check fn package_function() reference – see run()'s Advanced section . item commits successfully, batchit leaves small bookkeeping file named .batchit__<item id>, record run finished writing every declared file. ONE marker per item, directory item's lexicographically first declared output path – beside outputs share directory, one . create, read, rely file name ; batchit manages entirely, overwrites cleanly next time item id committed.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/reference/run_and_write_files_atomically.html","id":"ref-examples","dir":"Reference","previous_headings":"","what":"Examples","title":"Run a function once per item, in a fresh worker process, and have batchit write its output files safely — run_and_write_files_atomically","text":"","code":"if (FALSE) { # \\dontrun{ # style = \"return\": fn returns a named list, and batchit saves each # value to its declared path (as qs2 files). make_two_values <- function(x) list(squared = x^2, doubled = x * 2)  out_dir <- tempdir() result <- run_and_write_files_atomically(   fn = make_two_values,   items = list(list(x = 2), list(x = 3)),   outputs = list(     c(squared = file.path(out_dir, \"sq_1.qs2\"), doubled = file.path(out_dir, \"db_1.qs2\")),     c(squared = file.path(out_dir, \"sq_2.qs2\"), doubled = file.path(out_dir, \"db_2.qs2\"))   ),   n_workers = 2 ) result qs2::qs_read(file.path(out_dir, \"sq_1.qs2\")) # 4  # style = \"staged_writer\": fn writes each output itself -- see # ?where_to_write_output for a complete example. } # }"},{"path":"https://papadopoulos-lab.github.io/batchit/reference/stream_from_parent_and_write_files_atomically.html","id":null,"dir":"Reference","previous_headings":"","what":"Like run_and_write_files_atomically(), but build each item lazily instead of all at once — stream_from_parent_and_write_files_atomically","title":"Like run_and_write_files_atomically(), but build each item lazily instead of all at once — stream_from_parent_and_write_files_atomically","text":"Use building full items list front (way run(), run_and_collect(), run_and_write_files_atomically() require) use much memory – example, item large data slice, far many items hold list . Instead items list, give ids vector producer(id) function builds one item's arguments time. batchit keeps min(2 * n_workers, length(ids)) items flight calls producer() one slots free, bounds many produced items exist . free slot idle worker: every worker busy, roughly n_workers items may already produced queued. Everything else works like run_and_write_files_atomically(): item's function runs background worker, declared output files written safely – see function's help page atomic-write guarantee two styles.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/reference/stream_from_parent_and_write_files_atomically.html","id":"ref-usage","dir":"Reference","previous_headings":"","what":"Usage","title":"Like run_and_write_files_atomically(), but build each item lazily instead of all at once — stream_from_parent_and_write_files_atomically","text":"","code":"stream_from_parent_and_write_files_atomically(   fn,   ids,   producer,   outputs,   style = \"return\",   n_workers,   dev_path = NULL,   p = NULL,   label = NULL,   timeout = .BATCH_DEFAULT_TIMEOUT )"},{"path":"https://papadopoulos-lab.github.io/batchit/reference/stream_from_parent_and_write_files_atomically.html","id":"arguments","dir":"Reference","previous_headings":"","what":"Arguments","title":"Like run_and_write_files_atomically(), but build each item lazily instead of all at once — stream_from_parent_and_write_files_atomically","text":"fn object package_function(), naming function installed package. inline function accepted . ids One id per item, order want items produced run. Must unique, non-missing values (coerced character). producer function one argument – item's id – builds returns one item: named list holding fn's arguments, exactly one element items run_and_write_files_atomically(). Called per id, R session (never worker), one min(2 * n_workers, length(ids)) -flight slots free – load build item's data inside function, rather calling stream_from_parent_and_write_files_atomically(). outputs list aligned ids: outputs[[]] item 's output map, named character vector c(<name> = <final path>). May instead named item id (name set ids, order). rules run_and_write_files_atomically()'s outputs: every path absolute; every destination absent existing plain file (directory symlink); every output path, across every item one call, unique. style \"return\" (target returns named list) \"staged_writer\" (target writes output via where_to_write_output() instead) – see run_and_write_files_atomically() means. value errors. n_workers Number persistent background workers (mirai daemons) run . dev_path source tree package named fn, loaded per worker devtools::load_all(), instead using installed package. Leave NULL (default) use installed package. path exist, match package, error – even turn items run. p progress callback, progressr progressor. label optional short label added progress message. timeout Maximum time, seconds, let one item run treated failed (6 hours default; Inf disables limit).","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/reference/stream_from_parent_and_write_files_atomically.html","id":"value","dir":"Reference","previous_headings":"","what":"Value","title":"Like run_and_write_files_atomically(), but build each item lazily instead of all at once — stream_from_parent_and_write_files_atomically","text":"list, named id, order ids: element describes item wrote – list(committed = <named character vector: output name -> final path written>, attempt = <internal per-item identifier; can ignore >). Never fn's raw return value.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/reference/stream_from_parent_and_write_files_atomically.html","id":"details","dir":"Reference","previous_headings":"","what":"Details","title":"Like run_and_write_files_atomically(), but build each item lazily instead of all at once — stream_from_parent_and_write_files_atomically","text":"Two restrictions specific function: fn must object package_function() – inline function accepted , unlike run_and_write_files_atomically() – requires mirai package installed. also way get raw return value back; output-file record described , exactly run_and_write_files_atomically().","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/reference/stream_from_parent_and_write_files_atomically.html","id":"advanced","dir":"Reference","previous_headings":"","what":"Advanced","title":"Like run_and_write_files_atomically(), but build each item lazily instead of all at once — stream_from_parent_and_write_files_atomically","text":"function runs workers persistent mirai daemons, private compute profile creates tears call – never touches resets daemon configuration already outside call. background worker loads package named fn , starts (per item). 2 * n_workers items flight , carrying timeout: producer() called -flight slot frees , keeps memory bounded – item hangs past timeout resolves error instead blocking others forever. run()/run_and_collect(), batchit set BLAS data.table thread counts; divide CPU cores across n_workers fn multi-threaded.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/reference/stream_from_parent_and_write_files_atomically.html","id":"ref-examples","dir":"Reference","previous_headings":"","what":"Examples","title":"Like run_and_write_files_atomically(), but build each item lazily instead of all at once — stream_from_parent_and_write_files_atomically","text":"","code":"if (FALSE) { # \\dontrun{ # `write_one_slice()` must live in an INSTALLED package -- this function # loads it by package name + function name (never by value, unlike the # other three dispatch functions), so it cannot be an inline function # defined at the console. Put it in your own package's R/ directory, # install the package, then replace \"yourpkg\" below with its name: # #   write_one_slice <- function(slice) list(main = slice)  out_dir <- tempdir() ids <- c(\"a\", \"b\", \"c\") stream_from_parent_and_write_files_atomically(   fn = package_function(\"yourpkg\", \"write_one_slice\"),   ids = ids,   producer = function(id) list(slice = toupper(id)),   outputs = setNames(     lapply(ids, function(id) c(main = file.path(out_dir, paste0(id, \".qs2\")))),     ids   ),   n_workers = 2 ) } # }"},{"path":"https://papadopoulos-lab.github.io/batchit/reference/where_to_write_output.html","id":null,"dir":"Reference","previous_headings":"","what":"Get the path to write one declared output to, inside a ","title":"Get the path to write one declared output to, inside a ","text":"use style = \"staged_writer\" run_and_write_files_atomically() stream_from_parent_and_write_files_atomically(), function return output – writes declared output , path function gives . Call where_to_write_output(name) output name declared outputs, write exactly path. read back, move , rename – batchit renames place, next declared outputs, function returns successfully.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/reference/where_to_write_output.html","id":"ref-usage","dir":"Reference","previous_headings":"","what":"Usage","title":"Get the path to write one declared output to, inside a ","text":"","code":"where_to_write_output(name)"},{"path":"https://papadopoulos-lab.github.io/batchit/reference/where_to_write_output.html","id":"arguments","dir":"Reference","previous_headings":"","what":"Arguments","title":"Get the path to write one declared output to, inside a ","text":"name declared output name write – must one item's outputs names.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/reference/where_to_write_output.html","id":"value","dir":"Reference","previous_headings":"","what":"Value","title":"Get the path to write one declared output to, inside a ","text":"single absolute path string. WRITE path; read back move/rename – batchit renames final destination every declared output staged.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/reference/where_to_write_output.html","id":"details","dir":"Reference","previous_headings":"","what":"Details","title":"Get the path to write one declared output to, inside a ","text":"Use style = \"return\" instead simpler function just build R objects return named list; batchit serializes one final path . Use \"staged_writer\" function already writes files – example, format qs2, via another package's writer – build whole object memory just hand batchit save . works function actually running inside batchit worker, style = \"staged_writer\" item. Calling time, asking name item declare outputs, error. function inline function (see run_and_write_files_atomically()'s fn argument), call batchit::where_to_write_output() – inline function may call packages' functions package-qualified form, batchit automatically attached. function instead lives installed package, either import where_to_write_output call package-qualified way.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/reference/where_to_write_output.html","id":"ref-examples","dir":"Reference","previous_headings":"","what":"Examples","title":"Get the path to write one declared output to, inside a ","text":"","code":"if (FALSE) { # \\dontrun{ write_two_files <- function(x) {   saveRDS(x^2, batchit::where_to_write_output(\"squared\"))   writeLines(as.character(x * 2), batchit::where_to_write_output(\"doubled\"))   invisible(NULL) # ignored by batchit for style = \"staged_writer\" }  out_dir <- tempdir() run_and_write_files_atomically(   fn = write_two_files,   items = list(list(x = 2), list(x = 3)),   outputs = list(     c(squared = file.path(out_dir, \"sq_1.rds\"), doubled = file.path(out_dir, \"db_1.txt\")),     c(squared = file.path(out_dir, \"sq_2.rds\"), doubled = file.path(out_dir, \"db_2.txt\"))   ),   style = \"staged_writer\",   n_workers = 2 ) readRDS(file.path(out_dir, \"sq_1.rds\")) # 4 readLines(file.path(out_dir, \"db_1.txt\")) # \"4\" } # }"},{"path":"https://papadopoulos-lab.github.io/batchit/reference/write_qs2_atomically.html","id":null,"dir":"Reference","previous_headings":"","what":"Atomically write an object to a qs2 file — write_qs2_atomically","title":"Atomically write an object to a qs2 file — write_qs2_atomically","text":"Writes uniquely-named temporary file directory path, renames place. Rename--place atomic POSIX filesystems (server-side atomic SMB/CIFS), interrupted write – SIGKILL, crash, dropped mount – leaves destination either absent complete, never truncated file later read halt . ... forwarded qs2::qs_save().","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/reference/write_qs2_atomically.html","id":"ref-usage","dir":"Reference","previous_headings":"","what":"Usage","title":"Atomically write an object to a qs2 file — write_qs2_atomically","text":"","code":"write_qs2_atomically(object, path, ...)"},{"path":"https://papadopoulos-lab.github.io/batchit/reference/write_qs2_atomically.html","id":"arguments","dir":"Reference","previous_headings":"","what":"Arguments","title":"Atomically write an object to a qs2 file — write_qs2_atomically","text":"object Object serialize. path Destination path. parent directory must already exist. ... Passed qs2::qs_save() (e.g. nthreads).","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/reference/write_qs2_atomically.html","id":"value","dir":"Reference","previous_headings":"","what":"Value","title":"Atomically write an object to a qs2 file — write_qs2_atomically","text":"path, invisibly.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/reference/write_qs2_atomically.html","id":"details","dir":"Reference","previous_headings":"","what":"Details","title":"Atomically write an object to a qs2 file — write_qs2_atomically","text":"parent directory path must already exist: function never creates . promise, stated tempting reading wrong: durability. file.rename() atomic respect readers; fsync. power loss can still lose renamed file whose data reached disk. protects killed process, killed machine. lock. Two concurrent writers path produce complete file last rename wins. reader sees torn file, nothing decides writer won. always clean . partial temp file removed R-level error, .exit() run SIGKILL – hard-killed worker leaves randomly-named .tmp behind. destination still absent--complete, guarantee matters; litter . temporary file created tempfile() destination directory rather paste0(path, \".tmp\", Sys.getpid()). PID suffix collision-proof: PIDs unique among live processes one host, data kind commonly lives share two hosts mount – PID two machines pick temp path target. directory required: file.rename() atomic across filesystems. standalone writer, independent run_and_write_files_atomically() commit engine: temp carries dispatch attempt token swept engine's failure cleanup.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/news/index.html","id":"batchit-267291","dir":"Changelog","previous_headings":"","what":"batchit 26.7.29.1","title":"batchit 26.7.29.1","text":"New export: write_qs2_atomically(). standalone atomic qs2 writer, moved swereg’s qs2_write_atomic() semantics unchanged: serialize tempfile() destination’s directory, file.rename() place, destination either absent complete never truncated. ... forwarded qs2::qs_save(), supplied path comes back invisibly, partial temp removed write failed. roxygen carries three non-promises verbatim: durability (file.rename() fsync), lock (two concurrent writers produce complete file last rename wins), guaranteed cleanup (.exit() run SIGKILL, hard-killed worker leaves .tmp behind – destination still absent--complete). parent directory must already exist; function never creates . independent run_and_write_files_atomically() commit engine touch . file.rename() rather calling .batch_atomic_replace(), helper hard-codes batch commit: error prefix, standalone save must emit, documented ONE place commit temp becomes final. temp <basename(path)>.tmp<random> carries dispatch attempt token, .batch_sweep_task_temps() can never match . Version 26.7.29.1 rather 26.7.30: -day second release YY.M.D version needs fourth component express . version bump DESCRIPTION change. New tests tests/testthat/test-write_qs2_atomically.R.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/news/index.html","id":"batchit-26729","dir":"Changelog","previous_headings":"","what":"batchit 26.7.29","title":"batchit 26.7.29","text":"New vignette: “Choosing dispatch function” (vignettes/choosing--dispatch-function.Rmd). pkgdown site reference index articles. walks four dispatch functions worked examples, states two questions pick , covers two styles, inline fn versus package_function(), name-every-argument rule, atomic guarantee cover, failure behaviour. code changes: documentation . Chunks eval = FALSE output shown #> comments. Every example starts real subprocesses, streaming one needs function installed reader’s package, executing build time slow partly impossible. .github/workflows/R-CMD-check.yaml gains r-lib/actions/setup-pandoc@v2. R CMD check builds re-renders vignettes; pkgdown workflow already step, one , package shipped vignette now. DESCRIPTION gains VignetteBuilder: knitr knitr + rmarkdown Suggests. pkgdown site adopts shared house style. _pkgdown.yml switches bare template: bootstrap: 5 template: package: pkgdowntemplate – template package swereg uses – adds params: hero: block (overline, title, lede, two calls action pointing new vignette reference index), plus shared authors: footer/sidebar block. DESCRIPTION gains Config/Needs/website: papadopoulos-lab/pkgdowntemplate, pkgdown workflow installs (setup-r-dependencies needs: website). batchit ships logo, unlike swereg. hero’s art column auto-width template emits logo exists, band renders text-gap. Adding man/figures/logo.png (pkgdown::build_favicons()) populate later config change. CORRECTION: atomic-write guarantee documented stronger . README.md run_and_write_files_atomically() roxygen said failure – explicitly including rename failure – none item’s declared files replaced. false, .batch_commit_task()’s internal roxygen always said : step 5 replaces finals one time “never rolls back already-renamed final – crash two output renames leaves torn state”. three copies (vignette, README.md, roxygen) now state boundary phase: first replacement – error fn, \"return\" value whose names match, output \"staged_writer\" never wrote, failure serializing staged file – declared output touched; replacement loop – transactional; kill rename failure can leave set torn, valid marker vouching . individual file still never half-written, rename replacement atomic within filesystem; marker written – committed, even call reports failure, worker serializes result envelope committing. timeout explicitly confined first phase: measures worker’s whole lifetime can land mid-commit, R/batch.R’s comment notes mirai path. Four smaller documentation claims wrong way also corrected: producer() called -flight SLOT frees, worker idle (bound min(2 * n_workers, length(ids))); 64,000-byte / 100-line captured log tail belongs three processx functions streaming one; bare closure dev_path can name batchit’s source tree; .batchit__<id> marker sits directory item’s lexicographically first declared output, “beside outputs” share directory. behaviour changed – every edit R/ #' comment. Note release whole purely documentation: also adds CI step, changes DESCRIPTION metadata, changes pkgdown site configuration.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/news/index.html","id":"batchit-26720","dir":"Changelog","previous_headings":"","what":"batchit 26.7.20","title":"batchit 26.7.20","text":"Public API naming migration (v2) complete (PUBLIC_API.md). engine wire protocol unchanged; public exports renamed bare, self-documenting names: run / run_and_collect / run_and_write_files_atomically / stream_from_parent_and_write_files_atomically / package_function / where_to_write_output. batch_fn folded run/run_and_collect (fn argument takes package_function() descriptor bare closure); batch_record/batch_prior/batch_skip consumer-skip mechanism removed; per-item wire envelope now lightweight S3 batch_envelope (class + print + worker-check conformance test). BREAKING rename every export. See per-stage entries .","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/news/index.html","id":"batchit-26719","dir":"Changelog","previous_headings":"","what":"batchit 26.7.19","title":"batchit 26.7.19","text":"runner_package now required envelope field, consumer fallback. Previously worker validated meta$runner_package present , absent, fell back consumer package .batch_execute — malformed envelope make consumer namespace supply runner’s executor, exact runner/consumer confusion extraction seam exists prevent. shared .batch_check_envelope() also omitted runner_package, two transports accepted incomplete schema. Now: worker’s pre-load structural check treats runner_package like package/hash/id (non-empty string die code loads), worker resolves .batch_execute runner namespace unconditionally, .batch_check_envelope() requires runner_package . Regression tests cover checker real worker invocation envelope missing field.","code":""},{"path":"https://papadopoulos-lab.github.io/batchit/news/index.html","id":"batchit-26718","dir":"Changelog","previous_headings":"","what":"batchit 26.7.18","title":"batchit 26.7.18","text":"Initial release. batchit one subprocess dispatcher extracted swereg Phases 0–3 one-dispatcher project (see swereg’s PROJECT.md): design parent/child dispatch contract, correctness fixes every boundary defect, routing every legacy engine single dispatcher, done inside swereg first. Public API: package_function(package, symbol, version = NULL) — dispatch descriptor: package + symbol + srcref-independent digest(list(body, formals)) identity hash. Rejects ...-taking targets. child re-verifies hash refuses different code version dispatched. batch_run(target, items, n_workers, ...) — shape : fresh subprocess per item via processx, memory strategy memory-bound work. batch_stream(target, ids, producer, n_workers, ...) — shape B: lazy producer bounded backpressure via mirai daemons, items generated demand. frontends share one contract: -ends item validation target’s formals (every formal named, including optional ones; positional / duplicate / blank names), structured result envelope (protocol, id, status, value--error, executed-target identity, captured warnings surfaced parent), collect = FALSE drop values self-writing targets, per-item timeouts, bounded log tails failure, total, fail-closed inspector. extraction seam (one non-mechanical change vs. swereg, runner consumer package): worker script now always resolved runner package (system.file(\"batch_worker.R\", package = \"batchit\")); consumer’s dev_path supplies consumer’s code, never worker script. processx worker mirai daemon load consumer (via dev_path requireNamespace) runner (requireNamespace), skipping second load runner consumer coincide. meta$runner_package carries runner name across boundary. Every security property swereg preserved: exact [[ field extraction, pre-load structural check load-deciding fields (including required, non-empty runner_package 26.7.19), source-tree-vs-installed dev_path discrimination, child-side hash re-verification, total .batch_execute, total result inspector. test-batch_seam.R new swereg ancestor: builds installs throwaway consumer package dispatches real worker real mirai daemon runner = batchit consumer = seamtest, proving seam swereg-inside-swereg .","code":""}]
